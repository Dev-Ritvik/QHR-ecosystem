# FIXLOG

Running log of verified fixes. Append-only.

## [2026-07-15 22:11 IST] Issue: notify_lead_assigned ON CONFLICT cannot match partial dedupe index
- Found: `packages/db/migrations/0011_notifications.sql:36` — trigger function used bare `ON CONFLICT (dedupe_key) DO NOTHING`, but `notifications_dedupe_uq` is a **partial** unique index (`WHERE dedupe_key IS NOT NULL`). Postgres cannot infer a partial index from a bare conflict target, so the INSERT inside the trigger fails at plan time with error 42P10 — which aborts every lead INSERT with an assigned agent and every reassignment UPDATE.
- Why it matters: lead creation/assignment would hard-fail whenever the trigger fires — core CRM flow broken, not just missing notifications.
- Fix applied: LIVE database function `core.notify_lead_assigned()` already contains the corrected form `ON CONFLICT (dedupe_key) WHERE dedupe_key IS NOT NULL DO NOTHING` (patch from prior session was applied live). This session synced the drifted migration file: edited `0011_notifications.sql:36` to the same corrected form so fresh deploys don't reintroduce the bug. No new migration number needed — live already correct; no app-level migration tracking table exists (only Supabase-internal auth/realtime/storage schemas), so editing the file breaks no checksums.
- Verified (all against LIVE db `dhmatgzlqoogsnerdfsl` via the repo's own `postgres` driver):
  1. `pg_get_functiondef('core.notify_lead_assigned')` — live def has the WHERE-predicate form; file now matches it byte-for-byte on that line.
  2. Repro probe: zero-row insert with bare `ON CONFLICT (dedupe_key)` → **ERROR 42P10** ("no unique or exclusion constraint matching the ON CONFLICT specification"); same insert with `WHERE dedupe_key IS NOT NULL` → no error. Confirms both the bug and the fix mechanism.
  3. End-to-end: inside a transaction, inserted a test lead with `assigned_agent_id` set → trigger fired, notification row created for the correct agent (`type=assigned_lead`, correct dedupe_key) → transaction rolled back, no live data changed.
- Flagged for review: **yes** — two things:
  1. **DECISION (stop-list #5, logged not asked):** kept the epoch suffix in `dedupe_key` (`assigned_lead_<lead_id>_<epoch>`). The live e2e test shows the epoch has microsecond precision (`…_1784133672.927601`), so the dedupe index effectively never dedupes assigned-lead notifications — every reassignment produces a new notification. That may be intended (notify on every reassignment) or spam (rapid reassign ping-pong). Minimal/safest option chosen: fix only the conflict clause, change no dedupe semantics. If you want per-(lead,agent) dedupe instead, that's a one-line change to the key construction — product call, review needed.
  2. **ENVIRONMENT WARNING:** the Supabase MCP connector in this session is authenticated to a *different, unrelated* project — `alzzeaigdyvrajouyxha` ("FederationOS", a sports app with no `core` schema and no crm_app role). The repo's real database is `dhmatgzlqoogsnerdfsl` (per `packages/db/.env`), reachable only via direct connection strings. Inspected FederationOS's migration list: all entries look native to that app — no evidence any CRM migration was accidentally applied there. But any tooling that uses the MCP to "apply migrations" for this repo will hit the wrong production database. Recommend re-pointing or removing that connector.

## [2026-07-15 22:18 IST] Issue: rls.test.ts exercised a fake RLS mechanism and a wrong result shape (priority item #1)
- Found: `packages/db/src/sql/rls.test.ts` — set context via `SET LOCAL request.jwt.claims`, but every policy in `sql/rls.sql` checks `current_setting('app.role')` / `app.user_id` (set via `set_config` in client-core.ts's authedQuery). The test also read `result.rows` from `tx.execute()`, which in drizzle-orm 0.45 + postgres-js returns a bare RowList array — `.rows` is undefined.
- Why it matters: the file never tested the real RLS mechanism at all; test 1 additionally had a vacuous-pass structure by design, and in practice both tests failed on the shape bug ("rows is not iterable" / "expected undefined to be defined" — captured in a real run before the fix), so CI signal on the security backstop was pure noise.
- Fix applied: rewrote the file to use `createAuthedCoreAccess().authedQuery` (the real set_config mechanism, matching `src/__tests__/rls-context.test.ts`), with its own deterministic fixtures (distinct `10000000-…` UUIDs and phone numbers to avoid concurrent-file collisions), superuser seed/cleanup, and non-vacuous assertions: agent A sees exactly their 2 leads and NOT agent B's lead nor an unassigned (NULL agent) lead; owner sees all 4. Drizzle select returns a plain array — no `.rows` anywhere.
- Verified: `pnpm vitest run src/sql/rls.test.ts` against the live DB — 2/2 passed (agent got exactly 2 of 4 seeded rows, owner got 4). Post-run live query confirmed 0 leftover fixture rows in core.leads/core.users. (Also had to repair the workspace: `pnpm install` — node_modules was missing @vitest/utils, so no test in packages/db could even launch before this.)
- Flagged for review: no. Note: the unassigned-lead case is new coverage rls-context.test.ts doesn't have; policy `leads_agent_scoping` correctly hides NULL-assigned leads from agents.

## [2026-07-15 22:22 IST] Issue: systemQuery claimed to bypass RLS but actually failed closed (priority item #2)
- Found: `packages/db/src/client-core.ts` — `systemQuery` ran `db.transaction(fn)` with no session context; docstring claimed it executes "without applying any RLS constraints". On the crm_app connection every role-scoped policy (`current_setting('app.role')`) evaluated false → zero rows / denied writes.
- Why it matters: any system-level job using it silently sees an empty database instead of full visibility.
- Fix applied: `systemQuery` now runs `set_config('app.role', 'owner', true)` inside the transaction before `fn`; docstring corrected to state it grants owner-scoped visibility and explicitly does NOT bypass Postgres-level RLS (policy-less tables stay default-deny). Added permanent regression test `src/__tests__/system-query.test.ts`.
- Verified: `pnpm vitest run src/__tests__/system-query.test.ts` against the live DB — 2/2 passed: systemQuery sees both seeded leads (incl. one with NULL agent); control query on the same crm_app connection with no context returns 0 rows (demonstrates the exact pre-fix failure). `pnpm typecheck` clean. Fixtures cleaned up by the test itself.
- Flagged for review: **yes** — adjacent to the security boundary (grants owner-level read context to all systemQuery callers, as prescribed in the priority list). Plus three related findings needing decisions/checkpoints:
  1. **STOP-LIST (RLS policies), awaiting checkpoint:** live DB has RLS ENABLED with **zero policies** on `core.presentation_devices` and `core.notifications` → default-deny for crm_app regardless of app.role. Consequences: (a) `devices/verify` (the ONLY systemQuery caller) still returns `{ok:false}` for every device even after this fix — NFR-S5 revocation endpoint is effectively "everything revoked" (currently 0 device rows live, so no user-visible impact yet); (b) the `notify_lead_assigned` trigger INSERT (not SECURITY DEFINER, runs as caller) would be RLS-denied when crm_app assigns a lead → lead assignment via the app would ERROR. Both likely belong to the 0009_missing_rls.sql question (priority item #4). Proposed fix requires CREATE POLICY — waiting for approval per stop-list #2.
  2. **Next-issue candidate (code, not stop-list):** none of the cron routes use systemQuery — all six (`daily-notifications`, `demo-export`, `digest`, `expire-holds`, `expiry-alerts`, `reconcile`) build raw `postgres(process.env.DATABASE_URL)` connections with no RLS context. Under the intended crm_app credentials (per apps/crm/.env.example) they fail closed the same way systemQuery did.
  3. **Deployment question for you:** if the deployed apps/crm `DATABASE_URL` is actually the `postgres` (table-owner) URL rather than crm_app, RLS is silently not enforced for the entire CRM app (owner bypasses non-FORCE RLS). Worth confirming what the production env var holds — I can only see .env.example intent.

## [2026-07-15 22:30 IST] Investigation (no changes applied): priority items #3, #4, #5 verified against live — all remaining fixes are stop-list, awaiting checkpoint
- Item #3 (bookings/site_visits agent scoping): CONFIRMED live. `bookings_agent_scoping` routes only through nullable `lead_id`; `bookings.agent_id` (NOT NULL) is ignored. Live `site_visits` has NO `lead_id` column — the live policy routes through the `site_visit_leads` junction, so a visit with no linked leads is invisible to its own agent (`site_visits.agent_id` NOT NULL, ignored). Also: `sql/rls.sql`'s site_visits policy references the nonexistent `lead_id` column — the file would ERROR if applied fresh (live got the junction version from 0009_missing_rls.sql instead). Data impact today: 0 rows in both tables, so nothing currently hidden — forward-looking gap only.
- Item #4 (migration numbering): RESOLVED as investigation. 0009_missing_rls.sql WAS applied live — all 9 of its policies exist in pg_policy. 0010_media.sql is NOT a dead duplicate of 0009_media.sql: it is a superset (idempotent core.media DDL plus projection.media_manifests, which exists live) — do NOT delete it. Nothing to delete; the number collisions (0009×2, 0010×2) are cosmetic since there is no tracking table, but renumbering is stop-list-adjacent so left for the checkpoint. Caveat: 0009_media.sql is the only file carrying `GRANT ... ON core.media TO crm_app` (0010_media.sql lacks it), so any future cleanup must preserve that grant.
- Item #5 (grants/default privileges): PARTIALLY confirmed. Live crm_app grants are already correct for the five append-only tables (audit_log, lead_events, payment_ledger, unit_status_events, commission_overrides = SELECT+INSERT only). But `core.office_settings` has ZERO privileges for crm_app (created in 0010_office_settings.sql with no grant — exactly the failure the missing `ALTER DEFAULT PRIVILEGES IN SCHEMA core` predicts), and no default ACL exists for schema core (only projection has one). Fix = GRANT + ALTER DEFAULT PRIVILEGES → stop-list #2, awaiting checkpoint.
- NEW (found during #2/#4): live DB has RLS ENABLED with ZERO policies on 15 core tables (geometry_versions, lead_interests, media, notifications, office_settings, pois, presentation_devices, price_versions, site_visit_units, unit_commercial_details, unit_geometries, unit_land_details, unit_luxury_details, unit_status_events, user_settings). No migration file enables RLS on any of them — this was done directly against the live DB. For crm_app these tables are default-deny: reads return zero rows, writes error. Notable consequences if the app runs as crm_app: media pipeline, notifications (incl. the assigned-lead trigger insert), office/user settings, unit detail pages, and the expire-holds cron's unit_status_events insert would all fail. Fix = CREATE POLICY per table → stop-list #2, awaiting checkpoint.
- Also pending (non-stop-list, deferred until the policy decision because its verification depends on it): all six cron routes build raw `postgres(process.env.DATABASE_URL)` connections with no RLS context instead of using systemQuery.

## [2026-07-16 00:25 IST] Issue: missing crm_app grants + default privileges in schema core (priority item #5) — APPROVED CHECKPOINT
- Found: `core.office_settings` had zero crm_app privileges (created in 0010_office_settings.sql with no GRANT); no `ALTER DEFAULT PRIVILEGES IN SCHEMA core` existed (only projection had one), so any future core table would repeat the failure.
- Why it matters: the office-settings feature was completely unreachable for the app role; the missing default ACL is the root cause class.
- Fix applied: new `packages/db/migrations/0013_grants.sql` — GRANT SELECT/INSERT/UPDATE/DELETE on office_settings to crm_app; `ALTER DEFAULT PRIVILEGES IN SCHEMA core GRANT ALL PRIVILEGES ON TABLES TO crm_app`; re-asserted REVOKE UPDATE/DELETE/TRUNCATE on the five append-only tables (payment_ledger, audit_log, unit_status_events, lead_events, commission_overrides). Applied to live as the postgres role.
- Verified (live): office_settings SELECT as crm_app succeeds (1 row); pg_default_acl for schema core now shows `{crm_app=arwdDxtm/postgres}`; all five append-only tables still deny UPDATE and DELETE for crm_app (checked via has_table_privilege).
- Flagged for review: yes (grants change — but pre-approved at checkpoint, applied exactly as specified).

## [2026-07-16 00:25 IST] Issue: 15 core tables RLS-enabled with zero policies + agent-scoping gaps (new finding + priority item #3) — APPROVED CHECKPOINT
- Found: live DB had RLS enabled (via dashboard/sweep, no migration file) with zero policies on 15 core tables → default-deny for crm_app (broken: notifications incl. the assigned-lead trigger, devices/verify, media, settings, unit detail tables, unit_status_events writes from crons). Separately (item #3): bookings_agent_scoping ignored the NOT NULL bookings.agent_id (nullable-lead_id path only) and site_visits_agent_scoping only routed through the site_visit_leads junction — and sql/rls.sql referenced a nonexistent site_visits.lead_id column.
- Why it matters: core CRM flows would hard-fail or silently return nothing for the app role; agents could not see their own bookings/visits when the lead linkage is absent.
- Fix applied: new `packages/db/migrations/0014_missing_policies.sql`, applied to live. Policies: notifications = SELECT/UPDATE scoped to owning user (with owner-context branch), INSERT open (`WITH CHECK (true)`) — one deliberate deviation from the approved per-table spec, because the trigger inserts notifications FOR OTHER USERS as the assigning actor and crons insert as system; a user-scoped WITH CHECK would have rejected exactly the flows this fix exists to repair. user_settings = owning user + owner branch (digest cron reads all users' settings). presentation_devices = owner-only. The other 12 tables = USING (true) per spec. Also in 0014: ALTER POLICY bookings_agent_scoping / site_visits_agent_scoping to check the direct agent_id column first, OR'd with the existing lead/junction paths. Updated `sql/rls.sql` to match final live state (incl. removing the nonexistent lead_id reference).
- Verified (live, as crm_app via set_config — 28/28 checks passed): trigger INSERT succeeds with actor=owner, recipient=agent; direct notification INSERT as agent succeeds; agent A sees own notifications (2), agent B sees zero of A's; presentation_devices returns the seeded device under owner context and zero rows with no context; agent sees own booking with NULL lead_id and own visit with no lead links, other agent sees neither; sweep SELECT on all 15 tables succeeds; append-only grants unchanged. Full packages/db vitest suite: 29/29 passed. All fixtures cleaned (0 leftovers).
- Flagged for review: yes (RLS policy changes — pre-approved at checkpoint). Review specifically the notifications INSERT `WITH CHECK (true)` deviation: any crm_app session can create a notification for any user. Alternative (require actor context match) breaks the trigger/cron flows; a middle option is a SECURITY DEFINER trigger function, which I avoided as a bigger security change than the checkpoint covered.

## [2026-07-16 00:35 IST] Issue: all six cron routes used raw context-less DB connections (+ latent 42P10 in their ON CONFLICT clauses) — APPROVED
- Found: `apps/crm/src/app/api/cron/{expire-holds,expiry-alerts,daily-notifications,digest,demo-export,reconcile}/route.ts` each built a fresh `postgres(process.env.DATABASE_URL!)` connection per request with no RLS context (fail-closed reads / denied writes under crm_app). Additionally, expire-holds (×3), expiry-alerts (×1) and daily-notifications (×3) used `onConflictDoNothing({ target: notifications.dedupeKey })`, which drizzle renders as bare `ON CONFLICT ("dedupe_key")` — error 42P10 against the partial unique index the moment any notification insert runs (same bug family as the notify_lead_assigned trigger).
- Why it matters: every scheduled job would either see an empty database or crash on its first notification write.
- Fix applied: all six routes now use the shared `systemQuery` from `@/server/db` (owner context, pooled connection, no per-request client/end). All seven `onConflictDoNothing` sites now pass `where: sql\`dedupe_key IS NOT NULL\`` so drizzle emits the required partial-index predicate. digest was restructured so email dispatch happens outside the DB transactions. reconcile's `db: any` became a typed transaction, which surfaced (and got a guard for) a possible-undefined `pubP`. Added a permanent regression test to `packages/db/src/__tests__/system-query.test.ts` for the exact cron dedupe-insert pattern.
- Verified:
  1. `pnpm vitest run src/__tests__/system-query.test.ts` — 3/3 passed live as crm_app, including double-insert with same dedupe_key → exactly 1 row (no 42P10, dedupe works).
  2. `pnpm typecheck` clean in apps/crm.
  3. END-TO-END: launched the real Next dev server (which uses apps/crm/.env.local's crm_app DATABASE_URL) with a test CRON_SECRET and hit the routes over HTTP: unauthorized request → 401; reconcile → `{"ok":true,"driftCount":0}`; daily-notifications → `{"ok":true}`; expire-holds → `{"ok":true}`; expiry-alerts → `{"ok":true,"message":"Expiry sweep complete"}`. digest (needs EMAIL_API_KEY) and demo-export (writes to Supabase Storage) were not invoked — their code paths are identical systemQuery+select conversions, typechecked.
  4. Post-run live sweep: 0 rows in notifications/leads/presentation_devices, 0 fixture users — no residue from any verification this session.
- Flagged for review: no. Note (answers the earlier deployment question, at least locally): `apps/crm/.env.local` points DATABASE_URL at the **crm_app** role — so the app genuinely runs under RLS and all of today's policy/grant fixes are load-bearing, not theoretical.

## [2026-07-16 02:05 IST] Issue: Better Auth drizzle adapter missing schema + non-uuid generated ids (BUG 1)
- Found: (a) `apps/crm/src/server/auth.ts` passed `schema: { user: schema.users, session: schema.sessions }` to drizzleAdapter — with `usePlural: true` the adapter resolves models as `schema["verifications"]`, `schema["accounts"]`, etc. (verified in @better-auth/drizzle-adapter@1.6.23 source), so keys `user`/`session` never matched and verifications/accounts/passkeys weren't passed at all; (b) deeper: `packages/db` had NO drizzle definitions for accounts/verifications/passkeys even though the live tables exist; (c) after fixing the schema, OTP verify 500'd with `invalid input syntax for type uuid: "eqeGWioXo3..."` — better-auth's default 32-char string ids vs the uuid `users.id`/`sessions.id` columns.
- Why it matters: login was completely broken — the CRM was unusable.
- Fix applied: added `accounts`, `verifications`, `passkeys` table definitions to `packages/db/src/schema/core/auth.ts` (shapes copied from the LIVE tables: text ids with gen_random_uuid() defaults, uuid user_id FKs ON DELETE CASCADE — verified via information_schema/pg_constraint, not assumed); auth.ts now passes `schema: coreSchema` (which spreads all auth exports under their plural keys); added `advanced: { database: { generateId: "uuid" } }` so better-auth emits crypto.randomUUID() — valid for both the uuid and text id columns.
- Verified: typecheck clean (packages/db + apps/crm). Killed two stale dev servers holding ports 3000/3001 (old code), started fresh on 3000 (matching BETTER_AUTH_URL). Full OTP flow in the real browser: submit `owner@localhost.dev` → `[DEV] Would send OTP 423590` in terminal → entered OTP → `POST /api/auth/sign-in/email-otp 200` → dashboard at `/` rendered "Logged in as Dev Owner" with all widgets loaded (their queries ran → authedQuery set_config with session.role worked). Zero errors in server log after the fix; the only 500 in the log is the pre-fix uuid failure.
- Flagged for review: no. Note: the login created one real session row for the Dev Owner user (normal operational data, 30-day expiry).

## [2026-07-16 02:20 IST] Issue: public site / 404 — middleware rewrite targets didn't exist (BUG 2)
- Found: this copy was in an intermediate state, different from the bug notes: `_site`/`_present` folders did NOT exist and `middleware.ts` was ALREADY rewriting to `/site-home` and `/present-home` (matcher already updated too) — but those folders were missing. Instead, both `(site)/page.tsx` and `(present)/page.tsx` sat at their route-group roots, which is doubly broken: the middleware rewrite of `/` targeted a nonexistent route (404), and two parallel pages both resolving to `/` is itself a Next.js route conflict.
- Why it matters: the entire public site root was unreachable.
- Fix applied: moved `(site)/page.tsx` → `(site)/site-home/page.tsx` and `(present)/page.tsx` → `(present)/present-home/page.tsx` (both pages use only `@/` alias imports — verified before moving, no import breakage). No middleware changes needed — it already pointed at these names. Cleared `apps/public/.next` and restarted the dev server.
- Verified: dev server log shows `Compiling /site-home` (NOT `/_not-found`) and `GET / 200`; response body contains the real homepage ("Featured Properties" h1). Presentation host path also verified: `curl -H "Host: present.localhost:3001" /` → 200 with `Compiled /present-home` in the log. No route-conflict errors on startup.
- Flagged for review: no.

## [2026-07-16 03:05 IST] Issue: seed.ts ran DISABLE ROW LEVEL SECURITY against the PRODUCTION database — no guard existed (handover item 1)
- Found: `packages/db/src/seed/seed.ts:10` connected via `DATABASE_URL_MIGRATIONS || DATABASE_URL || localhost` — both env vars in packages/db/.env point at the REAL Supabase project, so the localhost "fallback" never applied. Lines 15-18 disabled RLS on 4 projection tables. Live inspection confirmed it HAD run against production: all 5 projection tables (incl. media_manifests) had RLS disabled, zero policies, and projects_pub/units_pub contained the seed's demo data (test-project/luxury-villas/budget-apartments + 200 dummy units) — the seed also DELETEs all rows first, so whatever was in those tables before is gone.
- Why it matters: a "test seed" script that mutates and de-secures production with no opt-in is exactly the kind of footgun that ends projects. (Nuance recorded honestly: no repo file ever enabled RLS on projection — ADR-002 isolation was grant-based, projection_reader = SELECT-only. RLS had been enabled live out-of-band with no policies, which broke the public site; the seed's DISABLE was a production-mutating workaround for that.)
- Fix applied: (1) seed.ts now refuses to run unless `DATABASE_URL_SEED` is explicitly set to a real value (placeholder-detection included) — no fallback to production URLs; the DISABLE statements are gone entirely (the postgres role owns the tables and bypasses non-FORCE RLS natively, so seeding never needed them). (2) New migration `0015_projection_rls.sql`, applied live: RLS ENABLED on all 5 projection tables with explicit policies — SELECT for projection_reader, ALL for crm_app (publish.ts/reconcile write projections).
- Verified (live): all 5 tables show RLS ENABLED with 2 policies; postgres (owner) still reads 3 projects with RLS on; real projection_reader login reads 3 projects/200 units and is DENIED INSERT (42501); crm_app UPDATE probe succeeded inside a rolled-back transaction; seed run without DATABASE_URL_SEED exits 1 with the refusal message (tested with production env vars loaded); public app (as projection_reader) serves / with all three projects rendered. packages/db typecheck clean.
- Flagged for review: **yes** — (a) RLS/policy change (user-directed revert; the two policies are additive design the repo never had — review the `USING (true)` choices); (b) DATA QUESTION: production projection tables currently hold seed demo data and their prior contents were deleted by earlier seed runs — restoring real data (re-publish from core, which has no published projects currently) is a decision for the user; (c) DATABASE_URL_SEED is still a placeholder — fill it with a staging DB to re-enable seeding.

## [2026-07-16 03:05 IST] Issue: writeAudit "tx-threading codemod" silently reverted at 21 of 52 call sites — incl. bookings, commissions, ledger (handover item 2)
- Found: `audit.ts` does accept an optional `tx` and inserts through it when given. But only 31/52 call sites pass tx (the ones physically inside authedQuery callbacks, e.g. leads.ts — verified genuinely threaded). The other 21 — including ALL FOUR booking actions, ALL FOUR commission actions, and the single payment-ledger append — call writeAudit AFTER the authedQuery transaction commits, with no tx. The deleted fix-tsc.js contained the embedded tsc output proving what happened: fix-audit.js blindly appended `, tx` even to call sites outside the callback scope ("Cannot find name 'tx'" at bookings.ts 105/170/235/300, commissions.ts 55/148/195/258, ledger.ts 49, etc.), and fix-tsc.js "fixed" the compile errors by REMOVING the tx from exactly those sites. tsc passing therefore proved the threading was deleted, not that it worked.
- Why it matters: at those 21 sites the audit entry is not atomic with the operation. No deadlock (the connection is released before writeAudit), but: an audit failure after commit leaves the operation unaudited AND — because the try/catch wraps both — returns an error to the user for an operation that actually committed (e.g. createBooking would report failure while the booking exists).
- Fix applied: none to the action files (money/commission/ledger paths are stop-list #3 — proposal below). Deleted the leftover codemod scripts `fix-audit.js` (repo root), `apps/crm/fix-audit.js`, `apps/crm/fix-tsc.js` (backed up to session scratchpad first; fix-tsc.js's hardcoded tsc output means a re-run would corrupt current files).
- Flagged for review: **yes — proposed fix awaiting checkpoint:** move each of the 21 writeAudit calls inside its enclosing authedQuery callback and pass tx (mechanical move; no amounts/calculation logic touched). Verification plan: exercise createBooking + appendLedgerEntry + commission override through the real UI/actions and confirm audit_log rows commit/roll back atomically with the operation.

## [2026-07-16 03:05 IST] Investigation: handover's "tickets still to implement" are stale (handover item 3)
- Found: T77 (commission engine: packages/domain/src/commissions/engine.ts + engine.test.ts, tranche math in integer paise), T78 (overrides: override.ts + OverrideCommissionDialog), T83 (owner dashboard: actions/dashboard.ts + 5 dashboard components + (app)/dashboard/page.tsx), T84 (brochure PDF: apps/public/src/app/api/brochure/[projectSlug]/[unitSlug]/route.ts) ALL exist, matching PROGRESS.md's [x] entries file-for-file.
- Action: nothing rebuilt. The handover doc predates completion of Phase 8 — treat its "Next Immediate Objectives" section as obsolete.

## [2026-07-16 15:55 IST] Issue: 21 writeAudit call sites ran outside their transactions (APPROVED, incl. money paths)
- Found: per the prior investigation, 21 of 52 writeAudit call sites called the helper after (or in one case before) the operation's authedQuery transaction, so the audit entry ran on a separate connection/transaction.
- Why it matters: audit entries were not atomic with the operations they record; a post-commit audit failure also made actions report failure for work that had actually committed.
- Fix applied MANUALLY, file by file (no codemod): moved each call inside its enclosing authedQuery callback and passed `tx`, switching payload references from the returned `result.*` values to the in-scope rows. Files: bookings.ts (4: create/cancel/default/convert), commissions.ts (4: rule update, entry generate — now inside the `toInsert.length > 0` branch so it only writes when entries are actually created — status update, override append), ledger.ts (1: append), devices.ts (2: enroll/revoke), holds.ts (2: client.create inside findOrCreateClient — this one was the ACTIVE DEADLOCK PATTERN, called inside createHold's tx while opening a second connection — and hold.extend), leads.ts (1: read_floor_price privileged-read audit moved into the read's transaction), media.ts (2: upload/reorder), pricing.ts (1: create_version), units.ts (1: unit save). No calculation logic touched anywhere.
- Verified: paren-matching sweep across apps/crm/src confirms 52/52 call sites now pass tx (0 unthreaded); `tsc --noEmit` clean in apps/crm; packages/db suite 30/30 passed against the live DB; packages/domain suite 133/133 passed. Money-path placement re-read line by line: every writeAudit in bookings/commissions/ledger now sits inside the `authedQuery(context, async (tx) => { ... })` callback, before the callback's return, ending in `}, tx)`.
- Flagged for review: yes (money-path files touched, as pre-approved). One BONUS pre-existing find fixed during verification: `packages/domain/src/commissions/engine.test.ts` first test used consideration `10000000000n` (10 crore in paise) while its comment and expectations assume 1 crore — a 10× TEST-FIXTURE typo (the engine itself is correct: cross-checked against the rounding test and the repo's 1-crore=1e9-paise convention in seed data). Changed the test input to `1000000000n`; domain suite went from 132/133 to 133/133. No engine code was modified — but given this project's history with a silent 10× bug in this exact area, worth your eyeball.

## [2026-07-16 17:30 IST] Issue: CRM build failure + auth bypass mock + missing drizzle relations (frontend blocker 1 — actual root causes differed from the bug notes)
- Found: the described "next/headers in a client bundle via digitizer/pois" error does NOT exist in this copy — webpack compiles clean. The REAL state: (a) `apps/crm/src/server/session.ts` `getSession()` was a hardcoded mock ("Temporary mock for deep crawler") returning an owner session unconditionally — a total auth bypass, and, because it removed the `headers()` call, authed pages became statically prerenderable; (b) `next build` then executed `/dashboard`'s DB queries at prerender time and failed with `Cannot read properties of undefined (reading 'referencedTable')`; (c) root cause of THAT: the schema package defined ZERO drizzle `relations()`, so every `db.query.X.find*({ with: ... })` in the codebase (dashboard, export, bookings page, generateCommissionEntries) throws at runtime.
- Fix applied: restored real `getSession` via `auth.api.getSession({ headers: headers() })`; added `import "server-only"` (package installed in apps/crm) so any future client-bundle inclusion fails loudly — the guard the bug notes wanted; added `packages/db/src/schema/core/relations.ts` (bookings→unit/client/lead/agent, holds→unit/client/lead, documents→project/unit/booking/client, leads→assignedAgent/client, paymentLedger→booking, units→project) spread into coreSchema.
- Verified: `pnpm build` completes (previously exit 1): compile ✓, all 25 pages generated ✓, /dashboard now dynamic. Real login enforced: unauthenticated /projects redirects to /login (observed in browser); OTP login works.
- Flagged for review: **yes** — the auth-bypass mock existed since some earlier session; anything "verified" through the crawler while it was in place was verified as a fake owner.

## [2026-07-16 17:30 IST] Issue: project detail page missing Publish wiring (frontend blocker 2 — partially stale notes) + unit creation completely broken
- Found: the nav tab bar ALREADY existed on the project detail page (`ProjectNav` — stale bug report), but no publish UI. A complete `PublishChecklist` component + `publishProjectAction` already existed, unmounted. Separately, discovered while running the e2e flow: unit creation was doubly broken — (a) `UnitForm.tsx` converted empty inputs to `null`, which zod `.optional()` rejects (and `''` fails enum/coerce.positive), so any blank optional field failed validation ("Invalid core fields"); (b) `units.ts` parsed asset-class details with the WRONG schema (`UnitSchema` core fields for all three branches) → detail fields silently dropped AND the upsert emitted `ON CONFLICT ... DO UPDATE SET` with an empty SET → SQL syntax error ("Failed to save unit").
- Fix applied: mounted `<PublishChecklist>` on the project detail page; UnitForm now omits empty fields instead of nulling; added `UnitLandDetailsSchema` / `UnitCommercialDetailsSchema` / `UnitLuxuryDetailsSchema` to validation.ts (keys match the detail tables; defaulted enums guarantee non-empty SET) and `saveUnit` parses each branch with its own schema and upserts the parsed data.
- Verified end-to-end in the real browser, zero typed URLs: /projects list → View/Edit link → detail page (tab bar + publish panel showing the REAL readiness checklist from checkPublishReadiness: hero/narrative/geometry/RERA pending, button correctly gated) → Units tab → Add Unit → saved unit E2E-101 (persisted: appears in inventory as available/200 sq yd) → Details tab back → publish panel. Digitizer and POIs pages both render fully. `tsc --noEmit` clean; full `pnpm build` green.
- Flagged for review: yes, two notes: (1) the created unit E2E-101 remains in the live "Dev Ritvik" project (delete if unwanted); (2) NEW money-adjacent bug found, NOT fixed (scope): UnitForm sends `overridePriceRupees` but UnitSchema expects `overridePricePaise` — the Override List Price (₹) field is silently ignored, and if wired naively would also be a 100× unit mismatch (rupees vs paise). Needs a decision + fix pass.

## [2026-07-16 09:10 IST] Issue: unit price override — rupees/paise mismatch at the form boundary (+ BigInt audit crash found by the new test) — APPROVED
- Found: `UnitForm.tsx` sent `overridePriceRupees`; `UnitSchema` expects `overridePricePaise` — zod strips unknown keys, so the Override List Price (₹) field was silently ignored. While testing the fix, the new money-path test exposed a second latent bug: `writeAudit` passes entity rows into jsonb `before`/`after` columns and `JSON.stringify` throws on BigInt — so any unit created WITH a persisted price override crashed the audit insert (never previously hit precisely because the override never persisted).
- Fix applied: (1) new `rupeesToPaise(rupees)` in `packages/domain/src/money/paise.ts` — parses the decimal TEXT (no float multiplication; rejects negatives, >2 decimals, malformed input). (2) New `apps/crm/src/lib/unit-payload.ts` `buildUnitPayload(FormData)` — the form's cleaning + rupee→paise conversion at the form boundary; UnitForm now uses it; the UI still displays/accepts rupees (edit mode already divided by 100). Schemas and DB stay paise-only, unchanged, per instruction. (3) `audit.ts` now serializes before/after through a BigInt-safe replacer (bigint → string), protecting all 52 writeAudit call sites. Also deleted unit E2E-101 from the live Dev Ritvik project (verified 0 rows, no dependents, details cascaded).
- Verified:
  1. `packages/domain` paise.test.ts extended with rupeesToPaise cases (whole, decimal ₹4500000.50→450000050n, 1 crore→1e9, rejects '-5'/'12.345'/'abc'): domain suite 136/136 green.
  2. NEW integration test `apps/crm/src/server/actions/unit-price-override.test.ts` (vitest added to apps/crm): builds the payload from real FormData via the real `buildUnitPayload`, calls the REAL `createUnit` server action as crm_app under RLS against the live DB (only Next runtime shims mocked), and asserts the persisted row: **typed ₹4500000.50 in the form → persisted override_price_paise = 450000050** (exact ×100, bigint compare). Fixtures self-cleaned; live residue check 0/0/0.
  3. tsc clean in apps/crm and packages/domain.
- Flagged for review: yes (money path — pre-approved). Note: the test first FAILED on the BigInt/jsonb audit crash (createUnit PERSIST_FAILED) — that failure is what surfaced fix (3); after it, green.

## [2026-07-17 ~10:30 IST] Feature: edit + delete for the Unit Inventory table
- Found (pre-work investigation, per request): FK delete behavior for everything referencing core.units, verified LIVE via pg_constraint — CASCADE: unit_land/commercial/luxury_details, media (unit-owned rows). NO ACTION (delete blocked by the DB): bookings, holds, documents, lead_events, lead_interests, site_visit_units, unit_geometries, unit_status_events. So the schema already BLOCKS deleting a unit with business history; only unit-owned rows cascade. Design follows the schema: the delete action pre-checks all eight NO ACTION referencers and returns a friendly "referenced by N x/y/z" message instead of a raw constraint error; no FK changes made.
- Built: (1) `deleteUnit(projectId, unitId)` server action in units.ts — dependent check, `unit.delete` audit (before-snapshot) inside the same transaction, delete; (2) edit route `units/[unitId]/edit/page.tsx` — loads unit + its asset-class detail row, merges flat, reuses UnitForm in update mode (updateUnit/saveUnit upsert path already existed); (3) UnitTable gains an Actions column with Edit link + DeleteUnitButton (confirm() dialog matching the Archive Project pattern, inline error display for blocked deletes).
- Verified (real browser, live DB, logged in as Dev Owner): created CRUD-T1 (150 sq yd) → Edit pre-filled correctly → changed to 175 sq yd / facing north → table shows updated values, same unit id; audit_log unit.update `before` = {150, facing null} proving the change. Delete-block path: seeded a unit_status_events row → Delete showed "Cannot delete unit CRUD-T1: it is referenced by 1 status history event(s)…" and the unit survived. Removed the fixture event → Delete succeeded: table empty, unit/details/events all 0 rows live, audit_log unit.delete captured the final state (175/north) as `before`. `tsc --noEmit` clean; full `pnpm build` green with the new `/units/[unitId]/edit` route in the manifest. No fixture residue.
- Flagged for review: no. Note: units with ANY status history are permanently undeletable by design (append-only unit_status_events + NO ACTION FK) — archiving is the path for retired units with history; flag if you want a soft-delete/archive action for units instead.

## [2026-07-18 ~15:30 IST] Batch: six frontend issues (unit edit failure, pricing JSON editor, POI reorder crash, POI auto-calc investigation, POI edit, map centering)
- **Item 2 — "Failed to save unit" on edit:** could NOT reproduce with normal edits (unchanged save and full edit incl. override both succeeded on current code). Real repro found: renaming a unit to an EXISTING number violates `units_project_number_live_uq` (UNIQUE project_id+unit_number among live units); the terminal showed the raw constraint failure and the generic catch returned "Failed to save unit". (An existing 23505 handler never fired because drizzle nests the Postgres code at `err.cause.code`, not `err.code`.) Fix: pre-check for a duplicate number inside the saveUnit transaction → `Unit number "X" already exists in this project` (VALIDATION_FAILED), plus a fixed 23505 backstop checking `err.cause?.code` for races. Verified in browser: renaming A-102 → B-501 now shows the friendly message; valid edits persist (A-102 dims/override updated live).
- **Item 3 — pricing JSON editor removed:** `PriceVersionForm` premiums textarea replaced with a "Corner Plot Premium (%)" input and repeatable Facing Premium rows (direction dropdown + % + add/remove). `buildPremiums()` emits the identical `{corner_pct, facing:{dir:pct}}` structure; `createPriceVersion`'s schema already accepted objects. Preview calculator now consumes the built object (no JSON.parse). Verified in browser on The Azure Residences: no JSON visible; preview computed ₹22,50,000 base and ₹22,95,000 with East facing selected (= exactly +2% from the labeled row). No new price version was activated against the live project (would supersede the user's real v1 — deliberately not exercised).
- **Item 4 — POI reorder crash:** reproduced with the exact terminal error. Root cause: inline `'use server'` closures on the POIs page — Next serializes closed-over values for bound inline actions and crashed on the captured postgres-js RowList; after switching the capture to a plain string array the serializer STILL crashed (error text tracked the code edit: `[function findIndex...]` → `[function indexOf...]`), i.e. this Next version's closure extraction itself is the hazard. Fix: eliminated inline closures entirely — new module-level `movePoi` action (fetches current order inside the tx, swaps, audits) + `movePoiForm`/`deletePoiForm` void wrappers bound with plain args. Side-fix: the POIs page/list now filters `archived_at IS NULL` (deletePoi archives; archived POIs were still rendering — the "three Apollo Hospitals" mystery). Verified in browser: ↑ on the second POI persisted (landmark POI sort 2→0 in live DB), zero serialization errors after the fix.
- **Item 5 — POI auto-calc (investigation only, per instruction):** the "Estimated Drive Time (min)" field is a plain manual input; NO auto-calculation exists anywhere — no routing API call, no API key/config for one (Mapbox/Google/OSRM absent from code and env). What IS auto-computed is straight-line distance (turf.distance from project centroid, server-side at create — now also on edit). To auto-calc drive time it needs a routing service (MapTiler is already configured for tiles but its Directions API would need enabling; alternatives: OSRM public server (no key, weak SLA), Mapbox Directions, Google Distance Matrix) OR a zero-dependency heuristic (straight-line × circuity ÷ avg speed, labeled "estimated"). NOT fixed pending the user's choice; meanwhile the misleading "Auto-calculated if blank" placeholder was changed to "Enter manually".
- **Item 6 — POI edit:** added `UpdatePoiSchema` + `updatePoi` action (recomputes distance only when the pin moves, audits before/after); PoiForm gained an edit mode (`?editPoi=<id>` pre-fills name/category/overrides/pin, Update + Cancel); POI names in the Curated List are now edit links. Verified in browser: opened the landmark POI, form pre-filled incl. stored pin coords, renamed → live row now "Apollo Hospital (Main)".
- **Item 7 — maps not anchored to the property:** both map surfaces (PoiMapPicker zoom 14, DigitizerCanvas zoom 16) ALREADY centered on `projects.centroid` — but centroid was NULL for every live project and NOTHING ever wrote it (no UI, no action, and the sole live geometry version has a NULL boundary; the digitizer page also passed RAW WKB where the canvas expects GeoJSON — parsed now). Fix: "Project Location" map picker in ProjectForm (writes `centroidLng/Lat` → `toEwktPoint` → `projects.centroid` on create/update; leaves centroid untouched when no pin chosen); geocoding fallback NOT implemented (needs an external API — flag if wanted). Also fixed a real picker bug this exposed: PoiMapPicker re-initialized the map on every parent re-render (effect depended on inline-callback identity), eating clicks — now initializes once with refs. Verified end-to-end in browser: clicked the map on Dev Ritvik → "Location set: 78.978, 21.033" → Save Changes → live `projects.centroid` = POINT(78.978 21.033).
- All: `tsc --noEmit` clean in apps/crm. Flagged for review: item 5 needs a product decision (routing API vs heuristic); item 3's activate-version path untested against live by design.

## [2026-07-18 ~17:00 IST] Batch: digitizer dead-end + control point editing + RERA checklist false-negative
- **RERA checklist (investigated from the real row, as instructed):** `checkPublishReadiness` (publish.ts) reads ONLY `projects.rera_number` for non-land projects. The form's "Approval Number" writes `approval_number`. Live Azure row confirmed the mismatch exactly: `approval_authority='rera'`, `approval_number='AP-RERA-2026-00452'`, `rera_number=''` → check fails on the empty dedicated column. Compounding it: ProjectForm carried `reraNumber` in state but RENDERED NO INPUT for it — `rera_number` was unfillable from the UI (empty string saved on every submit). Fix: (1) the check now accepts `rera_number` OR, when approval_authority='rera', the `approval_number` (both trimmed); (2) added the missing "RERA Number" input (shown for commercial/luxury) for the case where layout approval is DTCP/HMDA but a RERA registration exists. Verified by running the REAL `checkPublishReadiness` against the live Azure row (new regression test `apps/crm/src/server/publish-readiness.test.ts`): `approval: null` — and the full checklist is now all-green (user has since added hero/narrative/geometry).
- **Digitizer dead-end:** drafts are the only editable state (saveTransform/saveTracedUnits enforce it) and `uploadLayout` was the only draft creator — but its form renders only when NO version exists, so after activation both tabs were permanently blocked. Fix: new `createDraftVersion(projectId)` server action (clones the newest version's layout file + georef transform into a draft with versionNo max+1, refuses when a draft already exists, audited) + a "Revise Layout (New Draft)" button in the digitizer header (visible from both tabs whenever the latest version isn't draft, with a status hint). Panels now also gate their controls client-side on draft status with a read-only notice, instead of failing server-side.
- **Control points:** `SaveTransformSchema` control points gained an optional `label` (jsonb — no migration needed); GeoreferencePanel rewritten: editable name field per point (placeholder falls back to "Point N"), editable Lng/Lat number inputs per point, and a per-point "Move pin to current map selection" action; all editing disabled unless the version is draft, consistent with the server rule.
- Verified in the real browser against live: Azure's digitizer showed "v1 is active — create a draft to edit" + the button; clicking it created v2 DRAFT live (layout file cloned); the panel switched to editable Version 2; placed 3 control points via map clicks, named them ("NE corner", "Main gate", "SW boundary stone"), manually edited point 2's longitude to 83.30000, saved — live `georef_transform` contains all 3 labeled points with the edited value exactly 83.3. Demo control points then cleared from draft v2 (set back to NULL) so the user's revision starts clean; draft v2 itself left in place (that's the feature). `tsc --noEmit` clean.
- Flagged for review: no. Note: killed another stale dev server holding port 3000 (started 17-07 19:58, pre-dating current code) — if testing happens against that instance, already-fixed bugs will still appear broken.

## [2026-07-18 ~21:30 IST] Owner batch: items 1–9 done and verified; item 10 held for the owner's answer
- **1. Publish ST_Extent failure:** real error captured live as crm_app: 42704 `type "geometry" does not exist` (unqualified `::geometry` cast) and, once qualified, 42501 `permission denied for schema extensions` — PostGIS lives in the `extensions` schema, off crm_app's search_path (`"$user", public`) with no schema USAGE. No query-only fix exists; applied migration `0016_extensions_usage.sql` (GRANT USAGE ON SCHEMA extensions TO crm_app — grant sanctioned by the explicit publish-end-to-end instruction) + schema-qualified all app PostGIS calls (publish.ts ST_Extent — dropped the redundant cast; geometry.ts ST_SetSRID/ST_GeomFromGeoJSON ×2). NULL bbox for 0 traced geometries was already handled. VERIFIED: Azure Residences published end-to-end from the UI — core.published_at set, projection.projects_pub `the-azure-residences` + 3 units_pub rows live.
- **2. Trace & Link:** TRUE root cause found: `saveTracedUnits` passed a bare Polygon geometry to `validatePolygon`, which reads `.geometry` off a Feature → EVERY save failed "Polygon has no coordinates" on any version; PostGIS resolution was the next failure behind it; and non-draft versions additionally hit the known draft-only rule (now escapable via Revise Layout). Fixed the Feature wrapping; added per-polygon Delete and Redraw (next map shape replaces the polygon, amber highlight, cancellable) matching the POI edit pattern; trace drawing/save now gated client-side on draft status with a read-only notice. VERIFIED live on the user's v3 draft: drew 3, deleted 1, redrew 1 in place, assigned existing units A-102/B-501, saved — 2 real ST_Polygon rows (~117 m²) in unit_geometries; mock rows then removed.
- **3. Manual lead entry:** New Lead button on /leads + `/leads/new` page + NewLeadForm wired to the existing (previously UI-less) createLead action. VERIFIED: created "QA Walkin Lead" via the form — live row source=walk_in, stage=new, assigned to creator; cleaned up after.
- **4. Checkbox reassign:** per-row checkboxes + select-all (owner only) with a selection bar (count, agent picker, Reassign Selected, Clear); new `reassignSelectedLeads` action (owner-only, live-leads-only, assignment lead_events + audit with previous assignments captured) — separate from the existing all-of-agent flow. VERIFIED: selected the QA lead, reassigned Dev Owner → Test User; live row + assignment event confirmed.
- **5. Road width dropdown:** UnitForm free-text replaced with select 20/30/40/60 m + "Not specified" (also used in the bulk form). VERIFIED rendering with exactly those options. Note: an existing unit with a non-listed stored value (e.g. 25) will show "Not specified" until resaved.
- **6. Unit approval number optional:** input had no `required` and UnitLandDetailsSchema.approvalNumber was already optional — the owner's blocker was almost certainly the pre-existing land-save breakage fixed earlier (wrong detail schema → every land save failed). PROVEN both ways via new live-DB test `unit-land-approval.test.ts` through the real createUnit: empty → row saved with approval_number NULL; filled → 'LP-2026/173' persisted. 2/2 green.
- **7. Bulk unit creation:** "Bulk Add" button + `/units/bulk` page + `bulkCreateUnits` action: prefix/start/count (≤200) with live preview, shared facing/areas/road-width, per-class detail rows with schema defaults, price computed from the active version like single creation, whole-batch rejection listing colliding numbers, single audit entry. **DECISION (logged, not asked): land projects take ONE shared survey number applied to every generated plot** (edit per unit afterwards) — flagged for review as the asset-class ambiguity the batch mentioned. VERIFIED: QA-1…QA-3 created live (area 120, luxury detail rows), duplicate batch rejected with "These unit numbers already exist: QA-1, QA-2, QA-3…", fixtures cleaned.
- **8. Layout Type:** migration `0017_layout_type.sql` applied live — new enum core.layout_type (vmrda/panchayat/farmlands/suda/buda/dtcp/private_land/other), projects.layout_type column added, legacy mapping (dtcp→dtcp; hmda/rera/other→other), and rera_number backfilled from approval_number where authority was rera. **DECISION (logged): approval_authority COLUMN kept in place (deprecated, unread) — dropping a data-bearing column is stop-list; say the word and I'll add the DROP migration.** Every project-level reader updated: ProjectSchema, ProjectForm (new 8-value dropdown), publish readiness (land: layoutType+approvalNumber; non-land: rera_number as single source of truth), projection badge label. unit_land_details' own approval_authority usage intentionally untouched.
- **9. Approvals section:** shows only for VMRDA/SUDA/BUDA/DTCP; "RERA Approved?" toggle (pre-checked when rera_number exists) reveals the RERA input writing to rera_number; toggle off or other layout types → input/section completely absent from the DOM, and rera_number is cleared on save when hidden. VERIFIED in browser on Dev Ritvik: dropdown shows all 8 values; 'other'→no section; 'vmrda'→section+toggle checked+value 534345; toggle off→input gone; 'panchayat'→section gone; saved vmrda+toggle on → live layout_type='vmrda', rera_number retained. NOTE for review: publish still requires rera_number for ALL non-land projects — a luxury/commercial project with layout type Panchayat/Farmlands/Private Land now has no way to enter RERA and therefore cannot publish; tell me if those combinations should be publishable.
- **10. Unit status change:** NOT touched, per instruction — question posed to the owner in the report.
- All: tsc clean; apps/crm vitest 4/4 green; zero fixture residue (QA units/lead cleaned; orphaned dev servers killed).

## [2026-07-18 ~23:30 IST] Owner follow-up: RERA optional for publish + Mortgage unit status (items 8/9 amendment + item 10)
- **RERA optional (owner decision):** removed the non-land RERA requirement from checkPublishReadiness entirely — luxury/commercial projects on Panchayat/Farmlands/Private Land layouts publish without RERA; when rera_number exists it still renders as a public badge. Land still requires layoutType + approvalNumber. VERIFIED in browser on Dev Ritvik (luxury): toggled RERA off → rera_number cleared live ('' confirmed) → checklist shows only hero/narrative/geometry, no RERA line; RERA then restored (534345 confirmed live).
- **Mortgage status (owner answers: unit-level lien; in/out by CRM users; mortgage → available|booked; NEVER → sold; combined state is Booked/Advance Paid/RESERVED — registered was a typo):**
  - Migrations applied live: `0018_mortgage_status.sql` (ALTER TYPE core.unit_status ADD VALUE 'mortgage' — additive) and `0019_mortgage_transitions.sql` — the legal-transition matrix is enforced in BOTH the domain machine AND the `unit_status_events_legal_transition` DB CHECK constraint (discovered when the first transition attempt failed the check); constraint recreated as a strict superset adding available→mortgage and mortgage→available|booked.
  - Domain: machine.ts adds 'mortgage' with those transitions (mortgage→booked still requires a bookingId; mortgage→sold/registered impossible); presentation-label maps mortgage → public 'not_for_sale'; NEW `getOwnerStatusLabel` implements the owner's 4-state DISPLAY grouping (Available | Booked / Advance Paid / Reserved [on_hold+booked] | Sold Out [registered+sold] | Mortgage) — **display-only; internal legal states unchanged (logged decision: reversible grouping instead of destructive enum merge; 'not_for_sale' keeps its own label as it wasn't in the 4-state ask)**. 5 new machine tests; domain suite 148/148.
  - UI: TransitionDialog picks up mortgage automatically from LEGAL_TRANSITIONS; StatusBadge (amber for mortgage) and UnitTable now show owner-grouped labels.
  - VERIFIED end-to-end in browser on published Azure unit A-102: dialog offered mortgage from available → transitioned with reason → live status 'mortgage', status event recorded, badge "Mortgage", and the inline republish set the PUBLIC projection to not_for_sale; from mortgage the dialog offered EXACTLY [available, booked] (no sold/registered); released back to available → public projection recomputed (selling_fast); event history shows the full round trip. Unit restored to its original state.
  - **DECISION (logged): mortgage is enterable only from 'available'** (owner said "in/out" without specifying sources; booked/sold units entering mortgage seemed out of scope) — say the word if e.g. booked→mortgage is needed.
- All: domain 148/148, crm typecheck clean, no fixture residue, dev-server orphans cleared.

## [2026-07-19 ~00:30 IST] Owner follow-up: retire "Not For Sale" label + mortgage color amber
- Facts first: live DB has ZERO units with not_for_sale (all 4 units available), so retiring the label affects no data. The RED the client saw is the presentation-mode surface: StatusLegend.tsx listed "Not for Sale" at #ef4444 (red 500) and camera-presets colored not_for_sale plots red — and since a mortgaged unit projects publicly AS not_for_sale, mortgage rendered red there.
- Changes (owner wants exactly the 4 states; withheld-from-sale bucket = Mortgage):
  1. TransitionDialog: 'not_for_sale' filtered out of offered targets (stays legal in the machine + DB constraint for legacy compat — logged decision, no destructive enum removal).
  2. getOwnerStatusLabel: 'not_for_sale' now displays as 'Mortgage' (retired label; any legacy row shows under the Mortgage bucket).
  3. Colors: StatusLegend entry renamed 'Mortgage' at #d97706 (amber-600 — amber-500 was already taken by Selling Fast), camera-presets #ef4444→#d97706, plot-layers STATUS_COLORS.not_for_sale slate→#d97706, CRM StatusBadge not_for_sale red→amber (matching mortgage's amber).
  4. InventoryFunnel (owner dashboard) re-columned to the 4-state view: Available | Booked/Adv/Reserved (on_hold+booked) | Sold Out (sold+registered) | Mortgage (mortgage+not_for_sale) | Total — previously it also didn't count mortgage units in totals at all.
- Verified: typecheck clean in domain/crm/public, domain suite 148/148; in the running app an AVAILABLE unit's status dialog offers exactly [on_hold, booked, mortgage] — no "not for sale".

## [2026-07-19 ~17:45 IST] Owner batch: digitizer removal + detailed Booked/Sold Out form with documents
- **A. Digitizer tab removed (UI/route only, per instruction):** ProjectNav now Details | Units | Pricing | POIs | Commissions; deleted `app/(app)/projects/[projectId]/digitizer/` and `components/digitizer/` (canvas, georeference, trace, snapping). Server-side `actions/geometry.ts` + its validation schemas kept as dormant code (no UI references); DB tables geometry_versions/unit_geometries untouched as instructed. **Public-map dependency answer: nothing breaks.** The public 2.5D map reads `projection.geometry_pub`, which has 0 live rows — no published project renders plot geometry today; the 6 existing unit_geometries rows still project on future publishes if present. **Consequence (logged decision): the publish-readiness geometry requirement was REMOVED** — with no digitizer there is no way to produce geometry, so keeping the gate would have made every new project unpublishable. VERIFIED in browser: project nav shows exactly the 5 tabs, `/projects/<id>/digitizer` → 404, full walk project list → project → units → unit detail with no broken links; `tsc --noEmit` clean (stale `.next` types cleared).
- **B. Detailed Booked / Sold Out form (extends the REAL booking system — no parallel table):**
  - `CreateBookingSchema`: clientId now optional, alternative `newClient {name, phone E.164}` (refine requires one), optional `agentId`. `createBooking`: resolves buyer via the existing `findOrCreateClient` (exported from holds.ts, audits client.create inside the same tx), agent defaults to the actor, resolved ids used in the booking row + transition payload + status event; returns `bookingId`.
  - New `BookUnitDialog` on the unit page ("Mark Booked / Sold", shown for available/on_hold/mortgage): outcome radio Booked|Sold Out, buyer select-existing-or-create-new, agent dropdown (active agents+owners), token amount ₹, booking date, optional sale price ₹ (rupeesToPaise at the form boundary — no floats). Save → `createBooking` → `syncDealDocuments` (materializes the deal checklist); Sold Out additionally walks the LEGAL chain `convertBooking` (booked→registered, sets registered_on) → `transitionUnitStatusAction('sold')` — three real status events, no shortcut.
  - Unit page now shows at a glance: BOOKED BY / SOLD TO card (buyer name+phone, token, sale price, booking date, agent, registered-on, link to full booking) + a Booking Documents checklist (scope="booking") with upload + audited 60s signed-URL view, right on the unit. Manual TransitionDialog no longer offers bare 'booked' (a status flip with no booking row would strand the unit with no buyer/token/commission basis).
  - **VERIFIED end-to-end against live:** B-501 booked via the form with NEW client Kiran Test Buyer +919000000021, token ₹1,00,000, sale ₹58,00,000 → unit shows "Booked / Advance Paid / Reserved" + buyer card; uploaded a real PDF (Agreement of Sale) → ON FILE on the unit page; View File signed URL fetched the exact PDF bytes (200, %PDF) from the private bucket. B-502 sold via the form (Ravi Test Buyer, ₹5,00,000 token, ₹45,00,000 sale) → live chain available→booked→registered→sold, booking status converted, registered_on set, page shows "Sold Out / SOLD TO". Checklist TDS logic confirmed live: B-501 (₹58L) got Form 26QB, B-502 (₹45L) did not. Audit trail: client.create + booking.create (same tx timestamp), document.upload, document.view ×2 — all with actor.
  - **Infra fix required en route: the `private-docs` storage bucket did not exist in the live project** (upload failed 404 "Bucket not found" — the pipeline had never been exercised against live). Created it private via service role (additive). Also note: uploads fail with a TLS UNABLE_TO_VERIFY_LEAF_SIGNATURE error when the dev server runs under the sandboxed preview harness (env-injected proxy); a normally-launched `pnpm dev` works — environment quirk, not app code.
  - **Fixtures left IN PLACE deliberately** so the owner can inspect the result: B-501 booked (reversible in-app via Cancel Booking) and B-502 sold (terminal by design — reverting means deleting the booking/events via DB). Say the word and I'll clean both back to available.

## [2026-07-19 ~22:15 IST] Privileged Info panel: add + audited list of privileged entries
- **What "privileged info" means per the spec-as-built (no guessing needed):** there are no spec docs in the repo, but the schema itself defines it — `lead_events` rows of type `negotiation` (kind ∈ asked_price | client_offer | concession | counter, positive ₹ amount required by DB CHECK `lead_events_negotiation_shape`, optional unit + note) are hidden from agents by RLS policy `lead_events_negotiation` (owner-only), exactly like the floor price (which is DERIVED from them: lowest concession). So "adding privileged info" = adding negotiation entries; a free-text-only privileged note isn't representable without a schema + RLS change (flagged below).
- **Added:** (1) `getLeadPrivilegedEntries(leadId)` in actions/leads.ts — owner-only, FR-C21 audited read (`lead.read_privileged_entries` audit row commits in the same tx as the select, same as getLeadFloorPrice), returns entries with unit number + actor name. (2) `PrivilegedEntries` component in the Privileged Info panel: "Reveal Privileged Entries (Audited Read)" list + "Add Privileged Entry" form (4 kinds, ₹ amount via rupeesToPaise — no float multiply, optional unit of interest, confidential note) wired to the EXISTING `logNegotiation` action, so floor-price derivation and the `lead.negotiation` audit keep working unchanged. (3) **Decision (logged): negotiation events are now filtered OUT of the open Event Timeline** — they previously rendered there for owners unaudited, which made the audited-read pattern meaningless; privileged entries now surface only through the audited reveal. Say the word to restore them in the timeline.
- **VERIFIED live on lead "anitha rao":** added a Concession of ₹55,00,000 with a confidential note through the new form → live row `type='negotiation', kind='concession', amount_paise=550000000` (exact, no drift); Reveal listed it (kind, ₹55 L, note, actor "Dev Owner", timestamp, "Read audited" tag); Reveal Floor Price now returns ₹55 L — proving new entries feed the existing floor-price logic; timeline shows no negotiation event. Audit trail confirmed in core.audit_log: `lead.negotiation` → `lead.read_privileged_entries` → `lead.read_floor_price`, all with actor. Agent-side invisibility rests on the pre-existing RLS policy + the action's owner check (component only renders for owners). `tsc --noEmit` clean.
- **Flagged for review:** (1) the ₹55L test entry was left on lead "anitha rao" — it now IS that lead's floor price; tell me if you want it deleted. (2) If you want pure confidential NOTES (no amount) as privileged entries, that needs a small schema change (either relax the negotiation CHECK or a new privileged event type) plus an RLS policy update — both stop-list, so awaiting your go-ahead. (3) The stage-gated "Log Negotiation" section still exists separately and writes the same entries; it also has a pre-existing float-multiply (`parseFloat * 100`) that the new form avoids — happy to switch it to rupeesToPaise.

## [2026-07-19 ~22:45 IST] Fixture cleanup + Cancel Booking UI + negotiation money fix
- **Cancel Booking button added (needed to honor "use Cancel Booking"):** the `cancelBooking` action existed but had NO UI anywhere — my previous entry's "reversible in-app" claim was wrong until now. New `CancelBookingButton` (reason required, matching `bookings_cancel_needs_reason`) on the booking page header, rendered only while status='active'. This is the correct reversal path: it cancels the booking AND returns the unit to available in one transaction — the manual status dropdown would have stranded an active booking row.
- **B-501 reverted through that real UI:** booking 4967b286 now status='cancelled' with reason "Test fixture cleanup — booking created to verify the new Booked/Sold form"; unit back to 'available'; buyer card + booking-docs section gone from the unit page; Place on Hold / Mark Booked / Sold offered again. The uploaded test PDF stays attached to the cancelled booking's document row (uploads aren't destroyed by cancellation — by design). **B-502 left sold**, per instruction.
- **₹55L privileged test entry deleted** from lead "anitha rao" (superuser DELETE of the one negotiation lead_event — fixture cleanup, not an app path; audit rows kept, append-only). Verified in the app: Reveal Privileged Entries → "No privileged entries yet", Reveal Floor Price → "Not logged" (both still audited reads); 0 negotiation rows remain on the lead.
- **NegotiationLogger money fix (owner-approved):** `Math.round(parseFloat(amountStr) * 100)` → `Number(rupeesToPaise(amountStr))` with a friendly error on invalid input — same boundary conversion as the new PrivilegedEntries form. The practical hazard fixed is parseFloat's silent garbage acceptance (e.g. `parseFloat('55,00,000')` → 55 — a ₹55L entry becoming ₹55); rupeesToPaise rejects anything but digits + ≤2 decimals. VERIFIED by new live-DB test `negotiation-amount.test.ts` mirroring the unit-price-override test: typed '5512345.75' → rupeesToPaise → REAL logNegotiation as crm_app under RLS → live core.lead_events.amount_paise = 551234575 exactly; 1/1 green, fixtures self-clean. `tsc --noEmit` clean.
- Privileged-notes-without-amount deferred per owner ("revisit only if a real negotiation genuinely has no number").
- Noticed in passing (flagged as separate task, not fixed here): the booking page's own document checklists never fetch existingDocs, so it always shows "No documents synced yet" even when files are on file.

## [2026-07-19 ~23:00 IST] Booking page: document checklists were always empty
- **Root cause:** `bookings/[bookingId]/page.tsx` mounted `DocumentChecklist` for client and booking scope WITHOUT the `existingDocs` prop, which defaults to `[]` — so the page unconditionally rendered "No documents synced yet" no matter what rows/files existed. (The component takes server-fetched rows; only the unit page was fetching them.)
- **Fix:** the page's server component now queries `core.documents` inside `authedQuery` — (scope='booking', booking_id, archived_at IS NULL) and (scope='client', client_id, archived_at IS NULL) — same pattern the unit page uses, and passes each list as `existingDocs`.
- **VERIFIED in the browser** on B-501's booking (the one with a real upload): Client KYC checklist now shows PAN/Aadhaar rows, Deal Documents shows Allotment Letter + Form 26QB (missing) and **Agreement of Sale — ON FILE with View File**, and the View File signed URL fetched the actual PDF bytes (200, %PDF) from the private bucket. `tsc --noEmit` clean.

## [2026-07-19 ~23:59 IST] Inventory "—" prices: recompute on price-version activation (missing entirely)
- **Trace (exact answer to the question asked):** the table reads the STORED `units.computed_price_paise` (override wins if set) — no join to price_versions at render. Creating/editing a unit DOES compute against the active version (units.ts createUnit + bulkCreateUnits both do). What was missing: **activating a price version never recomputed existing units** — neither `createPriceVersion` (which auto-activates the new version) nor the dormant `activatePriceVersion` touched core.units. So any unit created BEFORE its project's version was activated stayed NULL forever. Live proof: Azure (units created after activation) 3/3 priced; lucky gardens 0/187 and Nirvik garden 0/40 priced despite both having an active v1 — units predate the version. The Pricing tab preview "worked" because it computes on the fly without persisting.
- **Fix:** new `recomputeUnitPrices(tx, projectId, version)` in actions/pricing.ts — recomputes every unit in the project via the same domain `computePrice`, sets computed_price_paise + price_version_id in ONE bulk `UPDATE … FROM (VALUES …)` (the first cut did per-unit UPDATEs: ~30s for 187 units over the pooler; bulk version ~4s incl. republish). Units that can't satisfy the rate basis (no area for per_sq_yd) are left NULL → still "—", correctly. Called from BOTH `createPriceVersion` (after insert) and `activatePriceVersion` (after activation, BEFORE the inline republish so the public projection gets the new prices); audit rows now carry unitsPriced/unitsSkipped; units page revalidated. Side-fix in `activatePriceVersion`: re-activating an older version now also clears its supersededAt (previously it stayed superseded and no reader would see it as active).
- **Backfill:** ran the REAL fixed `activatePriceVersion` (as crm_app under RLS, mocked owner session — one-off vitest runner, deleted after) against lucky gardens v1 and Nirvik garden v1: 181/187 and 40/40 priced (lucky's 6 unpriced units have NULL area — unpriceable until edited).
- **VERIFIED with hand math + browser:** lucky gardens A-101 (100 sq yd, north, corner; v1 = ₹15,000/sq yd, north 2%, corner 5%): 15,00,000 + 5% + 2% = ₹16,05,000 — table shows exactly that; A-102 (no corner) ₹15,30,000 ✓; the 6 area-less units still "—" ✓. Nirvik garden (north 3%): all 40 show ₹15,45,000 ✓. `tsc --noEmit` clean.

## [2026-07-19 ~23:59 IST] "Generate Checklist" PERSIST_FAILED — real error found, fix HELD for owner per instruction
- Reproduced live on Azure B-501; server log had the failed drizzle INSERT, and re-running it as crm_app surfaced the raw PG error: **23514 check violation `documents_exactly_one_owner`** — the constraint is `num_nonnulls(project_id, unit_id, booking_id, client_id) = 1`, and `initUnitChecklist` inserts rows with BOTH project_id AND unit_id set (documents.ts ~line 43: `projectId: parsed.data.projectId, unitId: parsed.data.unitId`). Unit-scope checklist generation has therefore NEVER worked; booking/client sync works because it sets exactly one column. NOT an RLS/permissions issue. Not fixed yet — reported to owner first, as instructed.

## [2026-07-20 ~00:20 IST] Generate Checklist fixed: unit_id-only insert (owner-approved)
- **Fix:** `initUnitChecklist` no longer sets project_id on the inserted rows — `documents_exactly_one_owner` requires exactly one of project_id/unit_id/booking_id/client_id, and unit-checklist rows are owned by the unit. No reader anywhere filtered unit docs by project_id (all use unit_id + scope), and `uploadDocument`'s update never touches owner columns, so nothing else changes.
- **VERIFIED end-to-end on Azure B-501:** Generate Checklist now succeeds — 4 luxury rows render (Agreement of Sale, Allotment Letter, OC, CC); uploaded a real PDF to Occupancy Certificate → ON FILE; View File signed URL returned the PDF bytes (200, %PDF) from `private-docs/units/<unitId>/…`. Live rows confirmed: all 4 have project_id NULL, unit_id set, `num_nonnulls(...) = 1`; scoping intact — B-501 has exactly 4 unit-scope rows, A-102/B-502 have 0 (still show the Generate empty state). `tsc --noEmit` clean.

## [2026-07-20] apps/public backend contract locked (Phase 1 execution — no page/component code beyond approved map infra)
- **Fixture cleanup (approved):** deleted `test-project`, `luxury-villas`, `budget-apartments` from projection ONLY — cascade removed their 200 units_pub rows (387→187); `core.projects` count 4 before AND after, and those slugs never existed in core. Origin identified: `apps/public/e2e/global-setup.ts` runs the seed script against `.env.local`'s DATABASE_URL — the LIVE db — flagged as a hazard (never run the default e2e suite against live).
- **2.5D map root causes fixed (approved):** (1) `NEXT_PUBLIC_MAPTILER_API_KEY` placeholder + get-a-key instructions added to `.env.local` (name verified against code); (2) new `getProjectMapData()` reader serializes geometry/centroid/POIs server-side via `extensions.ST_AsGeoJSON` — required migration `0020_projection_reader_extensions.sql` (GRANT USAGE ON SCHEMA extensions TO projection_reader; same 42501 class as 0016; boundary re-verified: core still denied); (3) new `SiteProjectMap` + `MapLocationFallback` — the fallback is a designed brand panel that still renders the location content (project, locality, POI distances), replacing the nonexistent-image fallback. Fatal-error policy: pre-load style/CSP/WebGL failures → fallback with a console.warn naming the reason; post-load tile/glyph failures stay cosmetic. Discovered en route: the app's own CSP restricts basemaps to *.maptiler.com (documented). Presentation mode untouched (ProjectMap.tsx, legacy readers, (present)/, middleware all unmodified).
- **VERIFIED in a real Chromium** (temp Playwright spec with a temp config that deliberately skips the live-seeding globalSetup): live-engine path mounts a canvas centred on the REAL centroid with the nearest POI rendered at its true position; fallback path renders the designed state with real POI content; glyph 403s correctly non-fatal. Temp route/spec/config/style deleted after; screenshots captured.
- **Four new readers built and executed against live as projection_reader** (`getProjectUnitSummaries`, `getAllPublishedUnits` (187 rows), `getLocalities`, `getAllMedia`) — real outputs captured via `apps/public/scripts/capture-contract.ts` (kept as the reproducible capture tool).
- **Contract locked:** `BACKEND_CONTRACT_FINAL.md` (repo root) — exact signatures, exported interfaces, real captured samples for all five new readers + SiteProjectMap contract + env contract + live-state table; FRONTEND_BLUEPRINT.md gained a §7 pointer. **Blueprint error found and corrected there: media `variants` really store `{h, w, url}`, not `{width, height, url}`.**
- **Final live state:** published = lucky-gardens (184 units, 183 available, POR pricing) + the-azure-residences (3 units, 1 available, public pricing); geometry_pub 0 rows (maps are location/connectivity mode until real geometry is published); Nirvik garden in core but unpublished — appears everywhere automatically on publish. `tsc --noEmit` clean in apps/public.

## [2026-08-02 ~15:30 IST] Hologram displays: flat plates were unreadable at eye level (client defect)
- Found: `holoplan_S1` / `holoplan_S2` were single textured quads lying horizontally at z=1.445. From a standing camera the grazing angle is ~6 deg, so perspective foreshortening crushed the layout to a line; from a side approach they vanished entirely. The client note was correct.
- Why it matters: the two layout stations are the commercial content of the hall — the plots a buyer is there to look at. Unreadable from the natural approach vector means the set piece does not work.
- Fix applied: three changes, all geometry rather than texture tricks. (1) **Rake 38 deg about the NEAR edge**, not the centre, so the near edge stays at its old height and the far edge lifts — pivoting about the centre would have dropped the near edge through the pedestal cap. Grazing angle goes ~6 deg → ~31 deg. (2) **Real extrusion**: `tools/blender/make_holo3d.py` reads each sheet as line art, finds the closed cells the linework bounds, classifies each by fill colour, and emits contours; `build_holo3d.py` turns them into prisms with top faces and side walls. Roads are deliberately NOT built — they stay at plate level and read as negative space between blocks, the way a physical model works. (3) **Upright callout cards** on leader rods, children of the station empty with the tilt cancelled, so they stay world-vertical and square to the approach vector however the table is posed.
- Six bugs found while building it, each caught by rendering and looking rather than by trusting the code:
  1. `(hier[0][ci][3] < 0 and rings or holes).append(pts)` — the classic falsy-list trap. `rings` starts empty, so `True and [] or holes` evaluates to `holes` and the FIRST (outer) contour of every cell was filed as a hole. Result: 0 cells kept, silently. Replaced with an explicit conditional.
  2. A global luminance cut cannot separate linework on both sheets at once — Kartikeya's plot dividers are LIGHTER than the green landscape beside them, so any threshold catching the dividers swallows the lawn. Switched to `cv2.adaptiveThreshold` (is this pixel darker than its neighbourhood), which is what linework actually is.
  3. Lucky Garden's green and blue plots are STATUS colours (available / sold), not terrain. The semantic hue rules were sinking them into the "landscape" and "water" layers. Split the config into `semantic` and `status` modes.
  4. Blanking the resort photo to white punched a hole in the linework it overlapped, draining ~16 plot cells into the page background. Fencing the blanked rects fixed that — but the fences then closed into cells themselves and extruded as ghost rectangles floating beside the model. Separated into three lists that had been conflated: `mask` (texture alpha), `fence` (wall ring), `nogeo` (no geometry).
  5. The texture and the geometry were masked from separate lists and drifted: the render still showed the Lucky Garden logo, the plot schedule and the resort photograph floating in the hologram. The texture is now emitted from the SAME mask list that drives the geometry.
  6. Leader rods are modelled along their own +Z and were given the flat cards' 90 deg stand-up, so they lay across the plan instead of rising from it. They need the station tilt CANCELLED, not the stand-up applied.
- Also corrected after looking at the first renders: blocks were 30mm tall on ~35mm plots (a pile of sugar cubes) — pulled back to 14–24mm; rim emission at 9.5 whited out the whole model in profile — reduced to 4.0 with a tighter power curve; sheet content reaching the sheet border lit the plate's 2mm solidify rim as a bright blade off the side of the model — a thin alpha margin now retires it.
- Verified: six Cycles frames at eye height (1.55–1.66 m) — square-on, oblique and hard profile for both stations, plus a room-wide context frame. The profile shot is the proof: the display keeps a silhouette instead of collapsing. Client approved and locked.
- Flagged for review: **yes** — "CLUB HOUSE" and "PARK" on S1 are MY reading of the drawing (it shows a pool with a structure, and playground equipment), not label text from the sheet. "LAKE" and both project titles are sourced. Strings live in `STATIONS` in `tools/blender/build_holo3d.py`. Also: Kartikeya's source is only 745×725 after crop — fine at these framings, the limiting factor for closer inserts.

## [2026-08-02 ~15:35 IST] pedestal_inlay_S1/S3/S4 were sitting on the open floor
- Found: measured `matrix_world` on the S1 inlay — bbox centred at (1.9, −5.95) while its pedestal is at (−5.95, −1.90). That is exactly the pedestal position rotated 90 deg. The meshes were authored in world space and then ALSO given the pedestal's yaw, so they were rotated a second time. S2 (yaw 0) was unaffected, which is why it had never been noticed.
- Why it matters: three 47cm dark stone plates floating at z≈0.95 in the middle of the entrance floor, in shot for any wide frame of the hall.
- Fix applied: zeroed `rotation_euler` on the three affected objects — the mesh data already carries the correct world position, so removing the duplicate rotation puts each back on its pedestal.
- Verified: re-measured world bounds for all four; each inlay now centres on its own pedestal.
- Flagged for review: no.

## [2026-08-02 ~17:30 IST] Baked GI + web export: four silent-failure traps in the Blender API
- Context: building `tools/blender/bake_lightmap.py` (decimate → lightmap UV atlas → Cycles GI bake) and `export_web_interior.py`. Recording the traps because each FAILED SILENTLY or reported success, and all four will bite anyone touching these scripts.
- **1. `nodes.new()` returns a pointer that goes stale.** Arming the bake target with `for nd in nt.nodes: nd.select = (nd is n)` matched nothing, because touching the node tree invalidates the reference `nodes.new()` handed back. The bake target was therefore never selected, Cycles baked a completely black atlas, and the only clue was an info-level "No active and selected image texture node" line buried in the log AFTER the whole bake had run. Compare node NAMES, not identities. The script now asserts the target is armed on every material and raises before baking.
- **2. The depsgraph does not evaluate modifiers on hidden objects.** Decimating `table_client` returned the original mesh unchanged while printing a plausible ratio (`18112->18112|ratio=0.331`). The object is `hide_viewport`, so the depsgraph never visits it. The decimator now unhides, evaluates, and restores.
- **3. Assigning `colorspace_settings.name` after writing `img.pixels` invalidates the buffer** and saves a black file. The sRGB transfer is now applied explicitly in numpy and the image left Non-Color, so Blender writes the numbers verbatim.
- **4. Linear 8-bit is the wrong container for a lightmap.** Normalised against its 99.5th percentile the room sits near 0.05 — value ≈13 in linear 8-bit, which bands visibly. The atlas is sRGB-encoded for precision and the divisor (4.6597) is published in the manifest as `lightMapIntensity`.
- Also: `smart_project`'s own packer left most of the square empty (147 objects of cornice and moulding produce mostly thin slivers) — a `pack_islands` pass with `shape_method='CONCAVE'` reclaims it.
- Architectural decision worth recording: **only the 147 unique shell objects are lightmapped.** The heavy repeats are already instanced — 153 anthemions off one 2.5k mesh, 54 balusters off one, 11 capitals off one — and a lightmap needs per-placement UVs, so lightmapping them would have destroyed the instancing that keeps the GPU upload at 214k vertices against 1.44M rendered. Ornament and props are lit at runtime instead.
- Correction to an earlier claim made in this session: unique geometry is **519k triangles, not 2.01M**. The 2.01M figure counts instances. Decimation was therefore applied to the shared DATABLOCKS, so one pass propagates through every instance (the capital mesh went 60k → 10k once and paid off 11 times).
- Verified: 4096 atlas, 1024 samples, OptiX. Drawn triangles 2.01M → 480k. Visual check of the atlas shows the coffered ceiling, wall panels with sconce falloff and the rosette all legible.
- Flagged for review: no.

## [2026-08-02 ~18:20 IST] Three scene defects found only because the exporter tripped over them
- **`EXT_HAZE` shipped in the GLB.** A 480m volumetric box that lives in the Scene Collection, NOT in `COL_Exterior` — so excluding the exterior never dropped it. It blew the exported scene bounds out to ±240m. The export now selects an explicit interior set rather than "everything visible".
- **The `KIT_` instancing masters exported as real geometry.** They are parked at z=−50 as sources and are not hidden, so they shipped as meshes sitting 50m under the floor. Same fix.
- **Both rug materials had the wrong normal map.** They drive a Bump node from `Carpet013_2K-PNG_Displacement` — a 16-bit greyscale HEIGHT map. Cycles is fine with that, but glTF has no bump node, so the exporter wrote the height map into `normalTexture`: wrong for shading, and unreadable to the texture toolchain (libvips rejects `I;16`). Both materials already carry a proper `NormalGL` map, so the export bypasses the bump and wires it through. **Export path only — `mansion_exterior.blend` is untouched.**
- Verified: post-fix scene bounds are 15.6 × 11.6 × 6.5 m (the actual hall); `Carpet013_2K-PNG_NormalGL` now occupies the normalTexture slot.
- Flagged for review: **yes** — the rug fix is applied at export. If the interactive build ever bakes from the .blend directly, fix the material there too.

## [2026-08-02 ~18:50 IST] GLB failed glTF validation; KTX2 toolchain version-mismatched
- Found: **`MAT_Wood_Dark` carried `roughnessFactor` 1.05**, against a spec maximum of 1.0. Clamping the Principled socket in Blender does NOT catch it — the value is not a socket default, it only exists once the exporter flattens the multiply chain that drives roughness. Also one normal-mapped primitive shipped without TANGENT, which the spec warns makes the tangent basis non-portable.
- Why it matters: an invalid asset is a loader-behaviour lottery across engines, and it was about to be committed as the deliverable.
- Fix applied: `tools/gltf/fix_ranges.py` clamps material scalars on the EXPORTED document (roughness, metallic, baseColor/emissive factors, occlusion strength, and the transmission/specular/clearcoat/sheen/iridescence/ior extension factors). `export_tangents=True` added to the exporter.
- Second problem, tooling: **`gltf-transform` 4.4.2 invokes `ktx create --assign-oetf`, which KTX-Software v5 renamed to `--assign-tf`**, so every texture failed to encode. Rather than pin an older KTX, `tools/gltf/encode_ktx2.py` drives `ktx` directly and wires `KHR_texture_basisu` by hand — which also allowed per-slot codec choice the wrapper does not expose usefully: **UASTC for normal maps** (ETC1S quantises a direction field hard enough to facet across large flat walls) and **ETC1S for everything else**, including the 4K atlas, where UASTC cost 7.1MB against 1.14MB for gradients nobody can see.
- Third problem, tooling: `gltf-transform`'s `resize`/`webp` shell out to sharp, and the libvips npx installs on this machine fails on EVERY png with "colourspace: parameter space not set". The texture stage runs in Pillow instead (`tools/gltf/optimize_textures.py`).
- Verified: **Khronos glTF-Validator 2.0.0-dev.3.9 — 0 errors.** All 56 warnings trace to that build predating `KHR_texture_basisu`: it cannot follow references made through the Basis and Draco extensions, so it reports the 28 images and 218 bufferViews they reach as unused and calls `image/ktx2` an invalid mime type. The extension is declared in both `extensionsUsed` and `extensionsRequired`, which is exactly what the spec requires. Recorded in `interior_hall.manifest.json` so it is not re-opened.
- Final: 235MB raw → **14.4MB**. Delivered to `apps/public/public/models/interior_hall.glb` with the Basis and Draco decoders in `public/basis/` and `public/draco/`.
- Flagged for review: **yes** — one primitive still carries a `TEXCOORD_1` its material does not sample. One unused attribute; not worth hand-editing the buffer.

## [2026-08-02 ~19:40 IST] CI had never run on this code; both jobs failed on first execution
- Context: the repo previously contained only a README. Pushing the project's history ran the workflow for the first time. Neither failure was a regression from the 3D work — both were latent, and each was hiding the next.
- **Lint: `next lint` opened its interactive setup prompt on a runner with no TTY.** There was no ESLint config or dependency anywhere in the repo, so `next lint` asked "How would you like to configure ESLint?" and died. Fix: added `eslint` + `eslint-config-next` and a `next/core-web-vitals` config to both apps, then fixed the 11 errors that surfaced — all `react/no-unescaped-entities` in user-facing copy, now proper typographic entities (`&ldquo;`, `&rsquo;`). Remaining findings are warnings (mostly `<img>` vs `next/image`) and do not fail the build.
- **Unit tests would have failed the moment lint was fixed.** Every spec in `packages/db` is an integration test that opens a real Postgres connection and needs both the superuser and crm_app roles — and they THROW AT IMPORT when the URLs are absent. The validate job provisions no database. Fix: `packages/db/vitest.config.ts` collects them only when a database is configured, reading `packages/db/.env` exactly as the specs do so local runs are unchanged.
- Also untracked `*.tsbuildinfo`. TypeScript's incremental build cache is a build artefact; the tracked copies made a rebase abort with "untracked working tree files would be overwritten".
- Verified: ran the full validate job locally with the database env removed, matching the runner — typecheck, lint and test all green. Then green on GitHub Actions.
- Flagged for review: **yes** — `packages/db` now runs ZERO tests in CI. The scaffolding to change that is in place: give the validate job a Postgres service and set `DATABASE_URL_MIGRATIONS` + `DATABASE_URL_CRM` and the specs run unchanged. That is a scope decision, not a CI fix.

## [2026-08-02 ~20:05 IST] The migration set could not build a database from scratch
- Found: provisioning a real Postgres for E2E killed the run on `type "media_kind" already exists`. A static pass over every `CREATE TYPE` / `CREATE TABLE` / `ADD CONSTRAINT` across all 23 migrations found **twelve objects created by more than one migration**, in two real groups plus one false positive.
- Why it matters: nobody could stand up a fresh environment. The schema had only ever existed through incremental application on machines that already had most of it.
- Fix applied: (1) **`0009_media.sql` removed** — it was a duplicate of the core-media half of `0010_media.sql` (same two enums, same table, same five constraints) and, alone among every migration in the repo, carried NO idempotency guards, so it could not even be re-run by itself. `0010` supersedes it and additionally covers the projection side. (2) **`0010_media.sql` guarded** where it re-created three objects `0007_projection.sql` already owns — the `pub_media_kind` enum, the `media_manifests` table and its foreign key — using the same `DO $$ ... EXCEPTION WHEN duplicate_object` pattern the rest of the repo uses, plus `IF NOT EXISTS` on the table and indexes.
- False positive worth recording so it is not "fixed" later: `unit_status_events_legal_transition` appears in both `0003` and `0019`, but `0019` DROPs it before re-adding to widen the check for mortgage states. That one is correct as written.
- Verified: the full migration loop applies cleanly to an empty PostGIS server in CI.
- Flagged for review: **yes** — `apply_migration.ts` keeps no record of what it has applied, so every run re-executes every file. Survivable now the statements are idempotent, but a migrations table would be better. This also supersedes the PROGRESS.md note about "duplicate migration numbers": `0009_media.sql` is gone, so `0009_missing_rls.sql` is now unambiguous; `0010_media.sql` + `0010_office_settings.sql` still share a prefix but create disjoint objects.

## [2026-08-02 ~20:25 IST] E2E: CI was not testing the database shape we deploy, and knocked on the wrong port
- **Migrations then died on `3F000 schema "extensions" does not exist`.** That schema is a Supabase convention — it installs PostGIS there rather than in `public` — and this codebase depends on the layout in two ways: two migrations grant USAGE on it (`0016` to crm_app, `0020` to projection_reader), and the application SQL is schema-qualified in five places (`publish.ts` ST_Extent, `geometry.ts` ST_SetSRID/ST_GeomFromGeoJSON, `projection.ts` ST_AsGeoJSON). A stock `postgis/postgis` image installs into `public` and has no such schema. **Relaxing the grants would only have moved the failure** — every `extensions.ST_*` call the app makes would then fail at runtime, and CI would have gone green against a database shape we never deploy. Fix: `packages/db/ci/bootstrap_extensions.sql` recreates the Supabase layout before the migration loop, so `0000_init`'s `CREATE EXTENSION IF NOT EXISTS postgis` becomes a no-op instead of installing into public first. It lives in `packages/db/ci/`, not `migrations/`, because it describes the managed platform rather than our schema.
- **Then every navigation failed with ERR_CONNECTION_REFUSED.** `apps/public` runs `next dev --port 3001`, and `playwright.config.ts` already health-checks `http://localhost:3001` before starting the suite — so the server was up and healthy while the tests knocked on **3000**. `PLAYWRIGHT_TEST_BASE_URL` was `http://present.localhost:3000`. The mismatch predates all of this work; it was in the workflow from the start. Fix: point it at 3001. The `present.` subdomain is deliberate — the Next middleware keys on it to route into the `(present)` segment, and Chromium resolves `*.localhost` to 127.0.0.1 per RFC 6761, so no hosts entry is needed on the runner.
- Also added `set -e` to the migration loop, which was letting a failed migration continue to the next file.
- Verified: **both CI jobs green on `main`** — `Typecheck, Lint, and Unit Tests` and `Playwright E2E (Merge Blocking)`.
- Flagged for review: no.

## [2026-08-02 ~20:35 IST] Build automation and its inputs were unversioned; repo now has a remote
- Found: the repo had **no git remote at all**, and 20 Blender/glTF pipeline scripts existed only on the local C: drive. The hologram pipeline that produces the S1/S2 displays was entirely unversioned.
- Why it matters: total loss exposure on a single machine, for the automation as well as the output.
- Fix applied: all 20 scripts committed to `tools/blender/` and `tools/gltf/`. Three that had silently drifted ahead of their repo copies were synced (`kit.py` gained `revolve()`, `prep_ornament.py` an explicit ROTX override, `preview_asset.py` a wider framing distance). Pushed to `github.com/Dev-Ritvik/QHR-ecosystem` (private), rebased onto the existing README commit rather than force-pushing over it.
- Also versioned the pipeline's **inputs**, which the scripts alone do not make reproducible: the two layout sheets and the original Kartikeya photograph, by narrowing the `assets/` ignore rule rather than dropping it (`assets/` is 1.9GB and stays out). The issued Lucky Garden PDF is deliberately NOT tracked — 26MB, larger than everything else in the repo combined, and `lucky_garden_raw.png` is the 300dpi render the pipeline actually reads.
- Flagged for review: **yes** — `decimate_chandelier.py` is still the one pipeline script that never received the world-transform bake fix its siblings have (the OBJ/FBX importers express Y-up→Z-up as an OBJECT rotation, so scripts measuring local `me.vertices` compute extents in the wrong frame). Nothing is broken in the scene today — it was corrected downstream by hand — but any asset re-run through it will come out rotated 90 degrees.

## [2026-08-27 22:45 IST] Exterior AO + material export: five defects that were invisible in Blender

- Context: the pre-bake geometry audit closed with `mansion_exterior_GEOMETRY-FINAL_20260827-1618.blend` (sha256 `411e4324…9942008`, re-verified byte-identical to the live file at the start of this session). Everything below is AO, material and export work on top of that lock. No vertex moved: a full re-dump of all 363 masonry objects against the lock file shows **0 bounding-box differences** and an identical loop count (184,632).

- **1. `StoneAO` was dead data, and `EXT_HAZE` is why.** Measured across all 100,679 loops of `mansion_walls`: p10 0.6192, median 0.62, p90 0.62. That flatness is `AO_FLOOR` from `build_stone_material_system.py` — the Cycles AO bake underneath it had returned 0.0 everywhere, so the only surviving signal was the floor plus the analytic ground-dirt ramp (0.434 at z=0 rising to 0.62 by z=1.70). Cause: `EXT_HAZE` is a **480 x 480 x 56 m box centred at z=26 with `hide_render=False`**, enclosing the entire building. Every AO ray hit it. Compounding it, `MAT_Stone_Wall`'s AO multiply sat at Factor **0** while Rustic/Trim/Paving/Steps sat at Factor **1** — so every cornice, portico, fountain and step was being multiplied by a flat ~0.62 carrying no AO information at all, while the wall was multiplied by nothing. The ornament was uniformly 38% muddier than the wall for no reason.

- **2. The Cycles AO bake operator cannot be trusted in this scene — replaced with raycasting.** `cupola_parapet_0` sits at z 7.60-7.96 with open sky above it and nothing within a metre of its outer face; a hemisphere raycast measures it **84% open**. The operator baked it to **0.000** — on GPU and on CPU, selected alone or with the set, with the object's own duplicate hidden, and at AO distances from 0.05 m to 20 m. The same duplicate moved to z 8.60 baked to 0.88 and at z 13.00 to 1.00. A fresh cube in clear air baked correctly (mean 0.861), so the operator is not globally broken; whatever it was integrating at that position was not the geometry that is there. Rather than keep guessing at it, `tools/blender/bake_ao_raycast.py` computes AO by deterministic cosine-weighted hemisphere raycast against an **explicit, auditable occluder set** — the same method every other geometric question in this project was settled with. Spot checks agree with independent measurement: parapet 0.802, west field 0.99, under the portico cornice 0.01, terrace at the wall base 0.552, open terrace 1.00.
  - **The big-face guard matters as much as the bake.** `mansion_walls` carries each facade field as a single n-gon of 57-86 m2; ten polygons are 43.7% of its whole surface. Those n-gons' corners are buried under the terrace and behind the eaves, so a corner-sampled vertex attribute renders the entire visible facade at ~7% brightness — the black-wall failure. Faces over 0.25 m2 are therefore shaded flat from the mean openness of the points on them that are actually **exposed** (a sample whose own outward ray cannot clear 50 mm is buried and gets no vote).
  - Applied masonry is deliberately **not** an occluder. Measured with it included, the four fields come out at 0.406/0.403/0.412/0.406 — all at the floor — but the south field n-gon also spans the smooth entrance bay, which measures 0.479 open inside the portico. One flat value cannot be both a joint bottom and the front door. It is also unnecessary: the runtime key is a shadow-casting directional at 10.6 degrees of elevation whose shadow map is 2048 px over a 52 m box (25 mm/texel), so it was never going to resolve an 18 mm joint whatever AO did.
  - Result, measured ON/OFF in **linear** (EXR, not display-referred): global min 0.404 = exactly the floor, reached by 0.1-0.8% of pixels. Localised where it should be — s6 (portico elevation) 25.1% of pixels below 0.7, hero 6.0%, west6 4.4%.

- **3. The glTF exporter is blind to the legacy Mix node, and it silently cost the tint and the AO.** Blender 5.2 reads base colour by walking back from the Base Color socket for `constant x colour attribute x texture`, and `search_node_tree.get_multiply_factors` matches that **only** through `node.type == 'MIX'` with `data_type == 'RGBA'`. All five stone materials used `ShaderNodeMixRGB`, whose type is `MIX_RGB`, with sockets `Color1`/`Color2` instead of `#A_Color`/`#B_Color`. Every test fails, so the exporter cannot see past the node. Measured on the export:
  - `MAT_Stone_Wall.baseColorFactor = [1, 1, 1, 1]` — the locked #E7E3DA tint (linear 0.799/0.768/0.701) simply gone.
  - `COLOR_0` = 255 on all 85,144 vertices of `mansion_walls`, with the real StoneAO landing in **COLOR_1** (ushort, min 26214/65535 = 0.400, the AO floor exactly). three.js binds COLOR_0 and ignores COLOR_1, so the AO shipped in the file and did nothing. The only warning was one info-level line: "The active Vertex Color will not be exported, as it is not used in the node tree of the material."
  - Fix: `tools/blender/fix_stone_material_export.py` converts them to `ShaderNodeMix` (RGBA/MULTIPLY/factor 1) — bit-for-bit the same arithmetic, so nothing about the Blender render changes. After: `baseColorFactor [0.7991, 0.7682, 0.7011, 1]` and COLOR_0 carrying 223 distinct values from 0.400 to 1.000. It also zeroes `MAT_Gold`'s Anisotropic (0.4 -> 0), which was shipping `KHR_materials_anisotropy` on geometry with no TANGENT — NaN in the anisotropic lobe, smeared by the bloom pass. That was being stripped downstream in `apply_ktx2_safe_textures.py`; it now never leaves Blender, and the extension is absent from the exported asset.

- **4. `export_web.py` would have shipped the mansion with no masonry.** `COL_Ashlar_West/East/North/South` are **siblings** of `COL_Exterior` at scene level — that is how the generator rebuilds them and how `survey_facades.py` hides them — and the exporter gathered `COL_Exterior.objects` non-recursively. A set is now a **list** of collections gathered via `all_objects`, with the parked `*_KIT` masters filtered by name and by z. The shipped `exterior_mansion.glb` shows the same shortfall from the other side: 212 nodes / 114 meshes against the current 479 / 382 — a difference of exactly 267 nodes. **The exterior currently in production predates the entire Phase-2 masonry.**

- **5. The 267 ashlar blocks had local UVs, which is why the wall and the blocks read as different stone.** `build_stone_material_system.py` states the rule the system depends on: "a world-axis box at exactly 1 UV unit per metre, with V on world Z". `mansion_walls` obeys it (u -8.05..8.05). The blocks did not: `ashlar_WEST_028` ran u -0.53..0.53, and across all 267 there were only **30 distinct UV origins**, so at the locked 3.33 m tile every block on the building sampled the same +/-16% window of Marble026, and any two blocks sharing a kit mesh were texturally identical. That is the pale mismatched panel visible around every window at REV_WEST6 and the flat slab at the south-west corner — not a stray object, just two different windows onto the same 2048px marble. `tools/blender/fix_masonry_uvs.py` re-projects the placed blocks in world space. World UVs are a function of world position, so this un-shares the ashlar kit meshes (78 -> 267, +7,560 loop-vertices, no change to draw calls, which were already one per node). The 96 **rustic** blocks were reverted to their 2 shared meshes: un-sharing them costs ~170k loop-vertices for a base course that is legitimately repetitive.
  - Trap worth recording: the two rustic kit meshes are `KIT_quoin_x_M` (long in X) and `KIT_quoin_x_M.001` (long in Y), and every rustic object has rotation (0,0,0) — the 90-degree turn lives in the **mesh**, not the transform. Stripping a `.001` suffix collapsed them into one and silently rotated 42 blocks. Correct mapping, confirmed against the lock and by run direction (f/b run along X at 0.703 m spacing, l/r along Y at 0.695 m): f/b -> `KIT_quoin_x_M`, l/r -> `KIT_quoin_x_M.001`.

- Verified against the exported asset, not the viewport (`tools/gltf/verify_export_qa.py`, new): 479 nodes / 382 meshes / 479 draw calls / 179,664 drawn triangles, 0 orphan nodes, 0 non-uniform or negative scales, 0 non-unit normals sampled, no missing or unreferenced images, `model_min.y = -0.0` (sits on the ground), only `MAT_Glass_Window` non-opaque, and COLOR_0 present on every stone material at 0.400-1.000. Container QA (`verify_final_glb.py`): 9.72 MB, Draco + KTX2 required, 26 images all 1024 square and multiple-of-4, no anisotropy materials, 0 negative texCoords.
- Restore point: `mansion_exterior_AO-MATERIAL_20260827-2243.blend`, sha256 `0e0a2ab1a38df363c9b9a61921231eadad761aa8cc16b611182d454924d840ce`, verified identical to live.
- Flagged for review: **yes** — two things. (a) The new asset is installed as `apps/public/public/models/exterior_mansion_v5.glb`; the production `exterior_mansion.glb` is untouched and `EXTERIOR_MODEL_URL` still points at it, so the swap is a one-line change awaiting sign-off. (b) 9.72 MB against production's 5.62 MB. That is honest cost, not regression: production carried 13 images because it predates the five-material stone system; this carries 26, of which 4.2 MB is UASTC normal maps. If the budget matters more than normal-map fidelity, encoding normals as ETC1S is the lever.

## [2026-08-28 07:55 IST] Exterior v5 shipped: runtime QA found two things Blender and gltf-transform both passed

- Context: `exterior_mansion_v5.glb` had already passed structural export QA. This pass took it through the actual web runtime, which is where the last two defects turned up. `EXTERIOR_MODEL_URL` now points at v5; the previous asset is untouched on disk and still addressable as `?model=prod`, which is the rollback.

- **Triangle-count reconciliation, because a report must not carry two totals.** 179,664 and 179,397 were both in circulation. Cross-tabulating four counting methods against both files (`tools/gltf/reconcile_triangles.py`) shows every method AGREES on each file — the verifiers never disagreed, the two numbers are two different files. The pipeline removes 267 triangles between the raw export and the shipped GLB: 14 cypress meshes at 16 each, `KIT_finial_tip_M` at 8 (shared by 4 nodes), and `mansion_walls` at 11. Measured on the raw export, those meshes contain **exactly 16, 8 and 11 zero-area triangles** respectively — Draco's degenerate-face pass, removing geometry that could never have been seen. `dedup` separately merges `door_handle_1`'s mesh into `door_handle_0` (−44 unique, 0 drawn), which closes the unique-count delta of −287. **Authoritative: 179,397 drawn triangles.**

- **1. `applyGrade` would have painted the rustication with the paving texture.** The paving grade cloned ONE material and assigned it to every mesh matching `/^(terrace_|rustic_|entry_step|entry_cheek|fount_)/` — correct when there was a single `MAT_Stone_Cream` to clone, damage the moment the stone was split. Against v5 it would have taken whichever of MAT_Stone_Paving / MAT_Stone_Rustic / MAT_Stone_Steps / MAT_Stone_Trim it met first and painted all 107 meshes with it, on top of a second darkening over the AO already in COLOR_0. Fixed by keying the clone on the SOURCE material — four clones instead of one, still not one per mesh.
  - Also caught `fount_water`, which is MAT_Water: transmissive, not a horizontal stone plate, and the grade was darkening the fountain. The clone now applies only to `MAT_Stone*`.

- **2. Disabling that grade outright was ALSO wrong, and only a measurement showed it.** The first fix skipped the grade entirely for split-stone assets, on the reasoning that the baked AO now does the job. Sampling the hero frame at fixed world points says otherwise:

    ```
                        production   v5 ungraded   v5 graded
        facade_west          87          94           94
        terrace_left         69         110           83
        terrace_front        56          83           64
    ```

    Ungraded, the terrace climbs ABOVE the west elevation — the same inverted hierarchy the tint was written to correct, with a different asset underneath it. AO answers "how enclosed is this surface"; the question here is "what is a horizontal plate worth against a lit wall at dusk", which occlusion cannot answer. Graded, v5 ranks `facade_south > facade_west > roof > terrace > spire > forecourt`, structurally identical to production and marginally better (the elevation now sits above the roof deck rather than tied with it).

- **UASTC normal maps kept, on measurement, not on principle.** An ETC1S-normals candidate builds to **3.63 MB against 9.72 MB** — the KTX2 payload drops 8.6 MB to 2.4 MB. Across the stone at the 6 m west camera the two are indistinguishable: mean absolute difference **0.425/255**. But the pixels that do differ are not scattered — 12 of the 12 worst 80px cells sit in one horizontal band at the bottom of the frame, which is the terrace paving, the only large flat surface in shot. That is exactly what `encode_ktx2.py` documents when it picks the codec: ETC1S "quantises the endpoints hard enough to produce visible faceting across large flat walls". So the 6 MB is real and so is the reason to leave it. The saving worth having is a MIXED policy — UASTC for `paving_normal` alone, ETC1S for the other seven normals (~6.7 MB of normal maps) — which is a pipeline change rather than a swap, and is left as a measured recommendation. The candidate is kept as `?model=v5etc1s` rather than deleted.

- **Runtime verification, in the browser, through the real page.** The Browser pane could not composite (`document.hidden`, rAF 0 ticks/s, R3F never mounted), so this ran under Playwright at 1440x900 where the canvas is 1424x900 and rAF ticks at 113 Hz. Measured with no query parameter at all, i.e. against the new default:
  - `exterior_mansion_v5.glb`, 9,953 KB, 166 ms; 267 ashlar; 96 rustic; **408 vertex-coloured meshes** against production's 1.
  - `mansion_walls` material live in three: colour **#e7e3da**, roughness 1, metalness 0, **normalScale 1.2**, `vertexColors: true`, COLOR_0 itemSize 3.
  - Glass transparent at opacity 0.55; gold metalness 1 with emissive #ffc98a; doors present.
  - Camera unchanged at `[-20, 15.5, 27] fov 41`; scene bbox identical to the asset it replaces, so nothing in `cameraPath.ts` moves.
  - **0 console errors, 0 warnings.** 71-81 fps against the old asset's 102 on the dev build — the cost is 267 extra nodes, i.e. the masonry itself, and it clears 60 fps with room.
  - The PRODUCTION build's frame rate could not be measured here and is not claimed: both assets read 1.0-1.3 fps on it, identically, and the cause is the harness, not the app. Reducing the viewport 9x (1424x900 -> 464x320) changed nothing, so it is not render cost; and rAF ticks 3 times per 2 s while setTimeout ticks 136 times, a 45x gap. The browser window had become occluded and Chromium throttled animation frames. The GPU is real (ANGLE / AMD Radeon / D3D11), not SwiftShader. Anyone re-running this must check rAF against setTimeout before believing an FPS figure.
  - Visual QA at hero, west 6 m, east 6 m, north 6 m, corner 6 m, entrance and south 12 m, driving the app's own scene and lights with the frameloop frozen. Coursing, joints, reveals, keystones, rusticated base, corner quoins and the full entrance composition all read, and the wall and the blocks read as ONE stone — the world-UV fix holding at runtime.
- Verified: typecheck exit 0; lint exit 0 (warnings only, none in the experience code); 48/48 vitest pass including the 29 in `cameraPath.test.ts` that assert the path against the model's solid volumes.
- NOT verified, and worth saying: the scroll-driven journey. SmoothScroll runs a virtual scroll that does not respond to synthetic `scrollTo` or `wheel` events, so the camera could not be driven along the path under Playwright. The hero beat is the app's own camera and was checked directly; the rest of the path is unchanged code against an identical bounding box.
- Flagged for review: **no** — but two standing notes. (a) `?model=prod` is the rollback and `exterior_mansion.glb` must not be deleted. (b) 9.72 MB against 5.62 MB is honest cost: production predates the five-material stone system and carried 13 textures against 26.

## [2026-08-28 08:15 IST] Runtime colour parity: the asset was never the cause

- Reported symptom: "the live website still visually shows the old colour/material appearance" against the Blender artist's approved renders. Traced before changing anything. The asset is not the cause and nothing in the material pipeline needed fixing.

- **The default route was already serving v5, and that is now proven from the built artefact rather than from source.** `EXTERIOR_MODEL_URL = '/models/exterior_mansion_v5.glb'`; no env var, `next.config`, CDN prefix, rewrite or preload overrides it; the only other mentions of the old filename in `apps/public/src` are two comments. Grepping the production chunks finds all three paths baked in (v5 default, prod rollback, etc1s candidate). From a FRESH browser context on the production build, the only model request is `/models/exterior_mansion_v5.glb`, HTTP 200, and the bytes the server returns hash to `d7a7e945…bc87c3`, identical to the file on disk. `legacyRequested: false` — no stale fallback, no second request.

- **Every layer of the material system survives into the runtime.** Measured live in three.js, not inferred:
  - `baseColorFactor` -> `material.color` = **#e7e3da**, linear (0.7991, 0.7682, 0.7011) — the locked tint.
  - roughness 1, metalness 0, `map.colorSpace` srgb, `normalMap.colorSpace` srgb-linear, 1024px with 11 mip levels.
  - `vertexColors: true`, COLOR_0 present as Float32 itemSize 3, min 0.400 max 1.000 — the AO payload, live on 408 meshes against the old asset's 1.
  - 267 ashlar, 96 rustic, 479 meshes, 179,397 triangles.

- **The texture is not shifted by the pipeline.** The shipped KTX2 was extracted from the GLB, decoded with `ktx extract`, and compared against Blender's `marble026/color.png`: mean sRGB (196.3, 172.3, 137.7) against (195.3, 172.7, 136.7) — **max channel delta 1.05/255, 0.41%**. Effective albedo (tint x texture, linear) is (0.4433, 0.3186, 0.1781) shipped against (0.4382, 0.3203, 0.1751) in Blender.

- **`normalScale` reads [1.2, -1.2] and that is CORRECT, not a defect.** three's GLTFLoader flips the green channel only under `useDerivativeTangents` — a normal map on geometry with no TANGENT attribute, where tangents come from screen-space derivatives. Our primitives ship POSITION/NORMAL/TEXCOORD_0/COLOR_0 and no TANGENT, so the flip fires by design (mrdoob/three.js#11438).

- **THE ACTUAL CAUSE: render transform and lighting rig, both authored, neither an asset property.**
  - three.js renders **ACESFilmic**; Blender's approved renders use **AgX**. Swept live at REV_WEST6 on the identical frame: Linear [87,78,64] L78.8 / Reinhard L75.1 / Cineon L79.2 / **ACES [84,72,55] L73.3** / **AgX [94,84,72] L85.6** / Neutral L59.5. ACES against AgX is +17% luma and a visible saturation shift (R-B of 29 against 22).
  - The site lights a **dusk rig** — key #FFB264 at 2.3 with shadows, counter #7FB4D6 at 0.3, hemisphere #4A6B96/#1A1512 at 0.36, ambient 0.06, `environmentIntensity` 0.45, fog #0A1120 from 26-105 m. The Blender review renders used a neutral sun (1.0, 0.94, 0.86) with a daylight HDRI at exposure -0.9.
  - **The stone pigment is identical.** Sampling mid-tone stone in both, Blender reads hue **34.3 deg**, sat 0.131, light 0.477; the live site reads hue **34.6 deg**, sat 0.240, light 0.300. **Hue delta 0.3 deg.** Same limestone, rendered darker and more saturated by a dusk rig and a contrastier curve.

- **Why it reads as "the old appearance".** Through the dusk rig the two assets differ by only 2-10% in mean frame luma: hero 66.55 -> 71.10 (+6.8%), west6 73.02 -> 74.35, corner6 110.61 -> 117.93, entrance 151.68 -> 154.51, east6 105.61 -> 107.39, north6 130.92 -> 143.95 (+10.0%), south12 125.86 -> 131.53. The assets ARE materially different — the old wall is `color #ffffff` with `vertexColors false`, v5 is `#e7e3da` with AO on 408 meshes — but the lighting compresses that difference at hero distance, where 25 mm of block relief is also about one pixel.

- Legitimate production benchmark, window verified active first (rAF 97/s against setTimeout 52/s): **90.7 fps** mean of 89.0 / 90.3 / 92.7 at 1440x900. JS heap 88.5 MB, DCL 523 ms, GLB fetch 78 ms. The earlier ~1 fps figure was an occluded-window rAF throttle and is void.
- Verified: production build exit 0 (BUILD_ID klPBiKW-VAu5sBcK26dJN); all three assets HTTP 200 with correct byte counts; console 0 errors / 0 warnings.
- Flagged for review: **yes, one art-direction fork.** Moving the renderer from ACESFilmic to AgX would bring the site materially closer to the approved Blender tonality, but it is a SITE-WIDE colour-management change — it desaturates the gold, the typography accent and the whole interior too, and WorldCanvas's exposure notes are written against the ACES curve. Not taken unilaterally.

## [2026-09-01 06:50 IST] Visual parity: the asset was right, the GRADE was the difference — and it is now addressable

- The prior pass closed on "COLOR PARITY: PASS" from a 0.3 degree stone-hue delta. That conclusion was overruled and the overrule was correct: technical asset parity is not visual parity. A 0.3 degree hue match on a sampled pigment says the two renders use the same limestone. It says nothing about whether the two IMAGES look alike, and they did not.

- **The camera question was a real confound and it is settled.** Blender `REV_HERO` sits at loc (-20,-27,15.5), lens 32.1 mm, vfov 41 deg, which converts to three-space (-20, 15.5, 27) fov 41 — EXACTLY the website hero camera. The Blender still I had previously put beside the site was rendered from `P3_CAM` (-26,-30,12) at 45 mm / 29.86 deg, a diagnostic camera created during the geometry sweep. Part of the mismatch the client saw was me comparing two different lenses. Re-rendered from REV_HERO before any conclusion was drawn.

- **Blender's conditions were READ from the scene, not inferred.** BLENDER_EEVEE; view transform AgX, look "AgX - Base Contrast", exposure 0.0 (the -0.9 in an earlier session was mine, not the artist's); world `W_HDRI` on `199_hdrmaps_com_free_2K.exr` at strength 1.0; `SUN_KEY` energy 3.2, colour (1, 0.93, 0.84), rotation (55.1, 0, 63.9) = 34.9 deg elevation.

- **ACES vs AgX was NOT the cause, and I had over-weighted it.** Ladder run live against the Blender ground truth (frame mean, 9 sampled regions, frameloop frozen, readPixels in the same task as the render):

    ```
        A  site as shipped                     63.9
        C  AgX view transform only             69.2   (+8%)
        D  exposure 2.2 only                   95.4   (background still 16.6)
        E  environment/sky only               145.5   (background 180.1)
        F  combined                           148.8
        -- Blender REV_HERO ground truth      101.1
    ```

    Swapping the view transform moves the frame 8%. The ENVIRONMENT moves it 128%. In the tuned grid ACES outscored AgX in every row and the two worst rows were both AgX, so the shipped tone mapping is kept unchanged. The dusk rig — a navy sky, fog from 34 m, a warm backlight and a 0.36 hemisphere — was the whole difference.

- **The brief does not mandate dusk.** masterprompt.md:428 asks for a "premium dusk/evening or editorial daylight grade depending on what works best with the actual asset". Both are in scope, which is why this ships as a fork rather than as a silent replacement.

- **Implemented as an addressable grade, default unchanged.** `?grade=daylight` extends the existing per-set `LOOK` / `useLook` mechanism that already carries `?exposure=&env=&ambient=` — no new architecture. Fitted by minimising RMS against the REV_HERO render across nine regions; best fit RMS 16.9. Sun placed where SUN_KEY actually is (34.9 deg elevation converted Blender (x,y,z) -> three (x,z,-y)); env 0.45->0.7, exposure 1.0->0.75, ambient 0.06->0.2, hemisphere 0.36->0.8, sky #0A1120->#6D7F6A, fog off, cool counter-key off.

- **Scoped by construction, and proven, not argued.** `<ExteriorLighting>` and the graded background only mount for `set === 'exterior'`, and `LOOK` was already per-set. Runtime proof: the interior route `/knowledge/reading-a-layout-plan` rendered with and without `?grade=daylight` is **pixel-identical — max absolute difference 0/255 across the full 1440x900 frame.**

- **The typography cost is real, smaller than I first measured, and it lands on ONE element.** Measured against a backdrop plate (the frame re-rendered with glyphs transparent, so the background is read rather than guessed between letterforms) — an earlier per-row estimate was measuring glyph pixels and produced false failures. Corrected figures, typical/worst backdrop:

    ```
                          DUSK          DAYLIGHT      AA needs
        H1 (66px)      12.87 / 7.26    7.53 / 5.62      3.0
        body (16.8px)   4.29 / 3.89    4.69 / 3.86      4.5
        gold link       5.33 / 4.21    5.35 / 4.81      4.5
        secondary link  3.55 / 3.23    3.72 / 3.37      4.5
        eyebrow         5.49 / 5.47    3.69 / 2.93      4.5
    ```

    A left-column scrim (0.38 at the edge, gone by 52% of frame width, so the building is never dimmed) pays most of it. Daylight ends up BETTER than dusk on body copy, gold and the secondary link, and worse on exactly one: the eyebrow, 5.49 -> 3.69.

- **DUSK IS NOT ACCESSIBLE EITHER, and the first version of my own code comment claimed it was.** On the shipping grade body copy reads 4.29 and the secondary link 3.55, both under AA. That is a pre-existing defect of 0.55/0.70-alpha type over a lit lawn, it is not caused by daylight, and the comment has been corrected to say so. The real fix is opacity on the small type, which is a typography change and outside this pass.

- Verified: typecheck exit 0; lint exit 0 (pre-existing warnings only, none in WorldCanvas); 48/48 vitest including the 29 cameraPath assertions; production build exit 0; 0 console errors and 0 warnings on both grades; default route still serves `exterior_mansion_v5.glb` (9433 KB, 360 ms) with no query parameter. Frame rate, rAF gated against setTimeout first (rAF 87-90/s, not throttled): **dusk 88.4 fps, daylight 89.9 fps** at 1424x900 — daylight is marginally FASTER, having no fog and one fewer contributing light.
- NOT verified: the scroll-driven journey past the hero beat. SmoothScroll's virtual scroll does not respond to synthetic wheel/scrollTo under automation, unchanged from the previous pass. The daylight grade alters no camera, no geometry and no bounding box, so `cameraPath` is untouched.
- Flagged for review: **yes — one art-direction decision, deliberately not taken unilaterally.** Daylight matches the approved Blender render and reveals the masonry, the slate roof and the site that dusk hides; dusk is what every lighting comment in WorldCanvas is written against and what the scrims were tuned for. The default remains dusk. Flipping it is one line in `GRADE_LOOK` plus making `'daylight'` the `useState` seed.

## [2026-09-01 07:20 IST] DAYLIGHT becomes the default exterior grade — decided on rendered images

- The previous entry shipped `?grade=daylight` as an addressable option with dusk still the default, and flagged the choice for review. This entry records the decision and the evidence. **Daylight is now the default; dusk is the rollback at `?grade=dusk`.** Neither grade was deleted.

- **Reconciliation first, because the premise was that this work was missing from GitHub.** It was not missing. `git fetch origin main` -> HEAD == origin/main == FETCH_HEAD == `dffcf2e`, 0 ahead / 0 behind, working tree clean. The daylight implementation and the parity FIXLOG entry were both already in that commit and already pushed. What made it invisible is the commit MESSAGE — "Add exterior model v5, stone material tools, QA screenshots, and blender scripts" — which names none of it, in a 123-file commit that is 93 PNGs. The source change was 1 of 2 `.tsx` files buried in it. Lesson recorded, not repeated: this commit contains only the grade work.

- **Judged on the rendered image, not on asset values.** The comparison was run on the RAW CANVAS with every DOM layer hidden, because Blender's render carries no typography and no scrim; comparing the composited page against it would have measured the UI, not the grade. Mean luma per region against the REV_HERO ground truth:

    ```
                      Blender      dusk            daylight
        facade          115.4      95.2  (-20.2)    123.9  (+8.5)
        roof slate      111.9      81.8  (-30.1)    113.6  (+1.7)
        terrace         134.6     120.1  (-14.5)    165.6 (+31.0)
        lawn             99.3      73.6  (-25.7)    100.1  (+0.8)
        fountain         63.8      40.5  (-23.3)     56.6  (-7.2)
        background       53.9      12.8  (-41.1)     99.1 (+45.2)
        spire            95.1      63.2  (-31.9)    103.7  (+8.6)
        -------------------------------------------------------
        RMS luma error             27.9              21.4
    ```

    And the limestone, which is the asset the whole sweep was about:

    ```
        Blender   H 36.1  S 0.133  L 0.436
        dusk      H 32.4  S 0.160  L 0.362    darker, more saturated
        daylight  H 34.1  S 0.150  L 0.469    closest on all three
    ```

    Daylight wins five regions of seven and EVERY architectural surface. Dusk is darker than the reference on all seven — it is not a different reading of the asset, it is uniformly under it.

- **Two residual gaps in daylight, named rather than buried.** The terrace runs +31 hot: its `PAVING_TINT` was fitted under dusk and is now doing the wrong amount of work. The sky runs +45 hot where Blender has a dark HDRI treeline and the site has a flat `#6D7F6A`. Both are real, both are follow-ups, neither blocks the default.

- **A third difference that NO grade fixes, and it is the largest remaining visual gap.** In the Blender reference the windows are DARK — recessed glass in shadow — and the gold finials are small and matte. On the site the windows carry emissive spill and the finials glow, in both grades. That is the authored `PHYSICAL SPILL FROM THE WINDOWS` rig plus the gold's emissive, not a tone-mapping or environment property. It reads as evening-interior-lit in a daylight frame. Flagged, not touched.

- **The canvas wrapper had to follow the grade too.** `<div className="bg-[#0A1120]">` sits directly behind the Canvas; left alone it showed navy under a daylight scene for any frame the GLB had not painted. It now uses the same expression as `<color attach="background">`. The Preloader and the `Surface` gradients are still brand navy and were NOT changed — that is a site-wide colour decision, not part of this pass.

- Verified on a FRESH production build (BUILD_ID cfH5kNmtej4Px7efI8w9M) and a fresh server and browser context: typecheck 0; lint 0 (pre-existing warnings only, none in WorldCanvas); 48/48 vitest incl. the 29 cameraPath assertions; production build 0; **0 console errors, 0 warnings** across default, `?grade=dusk` and the interior route. Default route with NO query parameter renders daylight and requests `exterior_mansion_v5.glb` (9433 KB). FPS with rAF gated against setTimeout first (rAF 181-183 per 2 s): **default/daylight 90.4, rollback/dusk 88.7.**
- **Regression proofs, measured not asserted.** `?grade=dusk` against the previously shipping dusk frame: mean absolute difference **0.029/255** (the 76 max is the animated motes field). Interior route before vs after the default flip: mean **0.030/255, max 2/255, zero pixels over 8** — the flip does not reach the interior.
- NOT verified: the scroll journey past the hero beat, unchanged from the previous pass — SmoothScroll's virtual scroll ignores synthetic events under automation. No camera, geometry or bounding box changed, so `cameraPath` is untouched and its 29 assertions still pass.
- Flagged for review: the three items above (terrace tint, flat sky, emissive windows/gold). The asset was not reopened: no Blender change, no re-export, no GLB touched.

## [2026-09-01 07:50 IST] Runtime visual-polish pass: terrace, sky, windows

- Three residual gaps were named in the previous entry and left as follow-ups. This pass closes all three at RUNTIME. No Blender edit, no re-export, no GLB, no masonry, no UV, no AO, no exporter change. The v5 asset is untouched.

- **Measured the same way as the parity pass**: raw canvas with every DOM layer hidden, against the approved REV_HERO render, because the reference carries no typography or scrim and comparing the composited page would measure the UI instead of the grade.

    ```
                      Blender    before     after
        facade          115.4     123.9     121.6   (+6.2)
        roof slate      111.9     113.6     113.2   (+1.3)
        terrace         134.6     165.6     139.6   (+5.0)
        lawn             99.3     100.1     100.0   (+0.7)
        fountain         63.8      56.6      54.8   (-9.0)
        background       53.9      99.1      62.4   (+8.5)
        spire            95.1     103.7     103.4   (+8.3)
        ---------------------------------------------------
        RMS luma error             21.4       6.4
    ```

- **1. TERRACE — 165.6 -> 139.6, and the facade did not move.** `PAVING_TINT` becomes per-grade: dusk keeps 0.48, daylight takes 0.32. The 0.48 was argued from a dusk sky — a horizontal plate sees a near-black sky while a wall sees the key — and that argument survives into daylight but the number does not, because the sky is now a light source the plate sees more of. The facade is a different material after the stone split, so darkening the paving cannot touch it: measured 123.9 -> 121.6, i.e. it did not move. Swept, not derived.

- **2. BACKGROUND — the flat colour was structurally incapable of being right.** Sampling the reference down its left margin shows the background is not sky: a 3/4 bird's-eye camera puts the horizon high and fills the top of frame with distant LAND — dark treeline L 44..48 to y=50, breaking into sunlit field L 112..117 by y=100. A single colour is necessarily too bright against the treeline and too dark against the field, and `#6D7F6A` was +45 and -16 respectively. Replaced with a 2x512 vertical ramp built on a canvas at runtime (no asset, no fetch, nothing for the CSP to refuse) assigned to `scene.background`, which three renders as a fullscreen quad for any non-env texture. Profile error after three iterations: -4.8, +0.1, +8.5, +1.6, -0.8, -0.6, +1.4, +3.7 across y 0..175. **Background only** — `scene.environment` is still the generated cube map, so this changes what is behind the building and lights nothing.
  - Stops are pre-compensated for ACES at exposure 0.75 and read brighter in source than the targets they hit. That is why they look wrong if read as colours rather than as pre-tone-map values.
  - A measurement error of mine is recorded here because it nearly sent the tuning backwards: the `bg_upper` sample region was placed when the background was FLAT, at a y-band that did not match the reference's. Once the background was graduated the region compared our open field against Blender's treeline and reported the better ramp as worse (RMS 11.6 -> 20.6). The region was corrected to the reference's own band and the ranking inverted to what the vertical profile had said all along.

- **3. WINDOWS — the largest remaining architectural mismatch, and it was two systems, not one.** The reference shows dark recessed glazing; ours glowed. Both contributors are now grade-gated:
  - `EMISSIVE` splits per grade. Daylight sets `MAT_Window_Interior` and `MAT_Wood_Dark` to 0. Every argument for the dusk values is an argument about darkness — the bloom threshold, the dark end of the orbit, a facade that would read as abandoned — and none of them apply at midday. `MAT_Gold` keeps a token 0.08: not for glow, since at metalness 1 against a 0.7 environment it has plenty to reflect, but because the finials are 40mm details at hero distance and zero loses the roofline.
  - The interior-spill rig in WorldCanvas is extinguished for daylight: both flanking rect-area lights and both point lights to 0, the entry bay held at 0.25 in a neutral `#FFE2C4` because the portico is the one place the key genuinely cannot reach under its own overhang.
  - The `__graded` guard became `__gradedFor: Grade`. `useGLTF` caches the parse and `scene.clone(true)` SHARES materials with it, so a sticky boolean would have pinned whichever grade mounted first.

- Verified on a fresh build (BUILD_ID R6wAgfc-CA5uipbcnF95M) and a fresh server and browser context: typecheck 0; lint 0 (pre-existing warnings only, none in the experience code); 48/48 vitest; production build 0; **0 console errors, 0 warnings** across default, `?grade=dusk` and the interior route.
- **Regression proofs.** DUSK rollback, canvas only, after vs before this pass: mean **0.043/255**, and the 0.199% of pixels that differ are spread over 84 of 160 grid cells with no cell holding more than 2.9% — scattered animation noise from the 2,400-particle motes field, not a cluster, so nothing structural moved. INTERIOR before vs after: mean **0.030/255, max 2/255, zero pixels over 8.**
- **Frame rate, and an honest caveat about the absolute number.** On this build, same conditions, back to back: **daylight 67.7 fps, dusk 65.1** — daylight remains the faster of the two, as it was before this pass (90.4 / 88.7). The absolute figures are ~25% below the earlier session on the same hardware; the rAF gate tracks the render rate exactly in both, and the change removes four lights rather than adding any, so the drop is accumulated machine load across a long session and NOT attributable to this work. Do not compare absolute FPS across sessions; compare the two grades within one.
- Flagged for review: the fountain sits 9 luma under the reference because the warm uplight that used to carry it is off. Restoring a warm light on a fountain at midday would be chasing a number at the expense of the image, so it is left. The remaining +6..8 on facade and spire are small and were not pursued for the same reason.

## [2026-09-02 19:31 IST] Phase 2.5B closed: the roof's lost base-colour multipliers, and a measurement basis that was wrong before it
- Found, and the order matters: **the comparison basis was invalid, so the defect it reported was not the defect that exists.** The Blender reference camera reproduced this page's `frameOffset` by crossing the forward vector with Blender's Z-up. three.js crosses with its own Y-up, and converting three (x,y,z) -> blender (x,-z,y) turns three's `right` into `(-fz, +fx, 0)` while the Z-up cross gives `(-fz, -fx, 0)` — **mirrored**, so the aim was pushed the wrong way and the building sat ~50px off. An edge-correlation alignment had returned dx=0/dy=0 and was believed; it was dominated by the hills and hedges, which are far away and therefore barely moved. Restricting the correlation to the building window exposes it.
- Why it matters: every roof number reported before this was measured between two differently-framed images through hand-placed boxes, one of which straddled the roof/cornice boundary and sampled cream stone. That is where "the roof is the second-brightest major surface" and "1.86x" came from. **Both were artefacts.** On a valid basis the roof is 5th of 11 and the ratio is 1.47.
- Fix applied, measurement side: camera reconstruction moved into three-space and converted afterwards (`right = cross(fwd, (0,1,0))`), and regions are no longer boxes at all — a **material-index pass** renders every material as a flat emission encoding its index in red, box filter width 0.01, Raw view transform, 1 sample, so the PNG's red channel is the material index exactly. Both images are sampled through that mask, eroded 3px to drop antialiased edges.
- Fix applied, defect side: `tools/gltf/bake_lost_basecolor.py`, and the candidate `exterior_mansion_v6_p25b.glb` (registered as `?model=p25b`, **not** promoted). Blender's slate base colour is `texture x (0.82, 0.86, 0.94) x (0.5 + 0.5 x AO)`, written as two legacy `ShaderNodeMixRGB` nodes. The exporter reads a base-colour chain only through the newer `ShaderNodeMix`, so it walked past both and wrote `baseColorFactor [1,1,1,1]` — 1.31x too bright in linear light. `fix_stone_material_export.py` had already converted the five `MAT_Stone_*` materials for this exact reason; the roof was never in its hardcoded list. Both multipliers are baked into a derived texture in linear light. `f=0.5` is half-strength AO and is baked as such.
- Verified:
  1. **Alignment, three independent ways.** Building-window NCC peaks at **dx=0, dy=0**; spire tip REF y=174 vs runtime y=173; mansion left edge 606 vs 606.
  2. **The camera was settled, not caught mid-lerp.** Two captures 6s apart differ by mean **0.046/255**, and the 0.13% of pixels that move are all below y=328 — the animated water. rAF was confirmed firing first; the in-app browser pane throttles it to zero when hidden and cannot produce a settled capture at all.
  3. **The arithmetic closes on a control, which is what makes it a diagnosis.** `MAT_Roof` carries the same slate texture with **no** multipliers in its graph, so its export was already faithful. At the lit end (p90, in linear) it matches Blender **1.018x** — so the lighting agrees there and any excess is albedo. `MAT_Roof_Slate` at the same end is **1.252x**, against 1.167 predicted from the tint alone (where AO -> 1) and 1.307 from tint x mean AO. It sits between them, where it must.
  4. **The encode is like-for-like, not a second opinion.** Re-encoding the ORIGINAL texture through the same LANCZOS-1024 resize and `ktx create --format R8G8B8_SRGB --encode basis-lz --qlevel 255` reproduces the bytes v5 already ships — identical length and sha256. The script asserts this and refuses to run if it ever stops being true.
  5. **The candidate is a one-texture edit, proven structurally.** 479 nodes, 381 meshes, 1785 accessors and the first **9,863,656 bytes** of the buffer are byte-identical to v5; `MAT_Roof_Slate` is the only material that differs. Running the committed tool on v5 reproduces the shipped candidate byte for byte (sha256 `643f0690…e914d3`).
  6. **Runtime A/B, daylight HERO.** Roof **74.5 -> 70.2** luma against Blender's 50.8; 95.9% of all changed pixels are inside the roof mask and 96.2% within a 3px collar of it, the remainder being the water band already shown to move between two frames of the same model. **Dusk HERO:** roof 104.1 -> 100.5, and `MAT_Stone_Wall`, `MAT_Stone_Trim`, `MAT_Stone_Paving`, `MAT_Ground` and `MAT_Roof` all move by **±0.01 or less**.
  7. typecheck 0; 48/48 vitest; lint 0 (pre-existing warnings only — the `ExteriorModel` one is a `useEffect` dep at line 619, untouched); production build 0; 0 console errors and 0 warnings. v5 still hashes `d7a7e945…bc87c3`, p34 is untouched, `EXTERIOR_MODEL_URL` still points at v5, and the locked source blend still has mtime 2026-08-27 22:43:35.
- **Two things found and deliberately NOT fixed**, both with numbers rather than opinions:
  1. **`MAT_Wood_Dark`'s lost multiplier points the other way.** `MIX` at Fac 0.68 against a constant (0.255, 0.165, 0.1) makes Blender's albedo **3.30x brighter** than the shipped texture — while the runtime already renders those doors **3.2x brighter** than Blender (41.0 vs 12.9). The two errors are in opposition. Baking it would take the doors to **76.5** and triple the error. The dominant term is missing contact occlusion on a deeply recessed door, not albedo. This is why the mandate's own instruction not to fix a mismatch before identifying its cause earns its place: the named fix was the wrong fix for that surface.
  2. **`MAT_Roof_Slate` loses its roughness remap too, and that is the rest of the gap.** Blender feeds the roughness map through a Map Range to **0.42..0.82** (LINEAR interpolation, so the Steps=4 field is inert); the exporter cannot express Map Range either, so it emitted the raw map with the default factor 1.0. **85.1% of the roof renders glossier than authored**, floor 0.118 against 0.467, mean 0.569 against 0.647. Measured on the shipped file, only **47.9%** of the roof's radiance is modulated by base colour — the other 52.1% is specular and IBL, which is why an albedo-only correction moves 74.5 to 70.2 and not to the 65.2 a pure multiply would predict. Fixing it is the same technique in a different slot (bake the remap into a derived metallicRoughness map) and is a separate authorisation.
- Flagged for review: **yes.** (a) The candidate is registered, not promoted — `EXTERIOR_MODEL_URL` still resolves v5 and the swap is a one-line change. (b) The roof closes 18% of its gap (+23.7 -> +19.4) and moves from 5th to 6th of 11 in the luminance hierarchy where Blender places it 7th; the remaining gap is the roughness export loss above plus the runtime's lack of ray-traced occlusion, which shows up on **every** material (`MAT_Stone_Trim`, with no export loss at all, still runs p10 28 -> 54) and is not something a base-colour edit can reach. (c) The blend and the GLB now diverge: the node graph still carries the legacy MixRGB chain, so a future re-export from source would lose this again unless it runs through the new tool. That is why the recipe is a committed script with a self-check rather than a one-off.

## [2026-09-03 13:55 IST] P2.5B.2: the roof's lost roughness remap - and two measurement instruments that were both wrong
- Found: `MAT_Roof_Slate` feeds its roughness map through a `Map Range` node, 0..1 -> **0.42..0.82**, LINEAR (so the `Steps=4` field is inert) and clamped. Map Range is no more exportable than the legacy MixRGB nodes P2.5B dealt with, so v5 shipped the raw map with the default factor: **85.2% of the roof renders glossier than authored**, floor 0.188 against 0.489, and a ceiling of 1.000 against an authored hard limit of 0.82.
- Why a `roughnessFactor` is NOT the fix, which was the obvious wrong move: glTF roughness is `factor x texture.G`, a pure MULTIPLY; Blender's remap is AFFINE. No factor can produce the 0.42 floor. The offset has to be baked into the texture, which leaves only the question of where the CEILING lives - and that is settled by measurement, not preference. Round-tripped through the real encoder and compared per texel against the float ideal:

  | representation | mean abs err | rms | max | resulting range |
  |---|---|---|---|---|
  | v5 as shipped | 0.11218 | 0.12363 | 0.309 | 0.188 .. **1.000** |
  | factor 1.00, whole span in G | 0.00869 | 0.01274 | 0.135 | 0.498 .. **0.847** |
  | **factor 0.82, G normalised** | **0.00815** | **0.01231** | **0.131** | 0.486 .. **0.820** |

  Factor 1.0 lets ETC1S noise reach **0.847, above a ceiling Map Range's clamp makes hard**; putting 0.82 in the factor caps it by construction and keeps 104 levels of 8-bit instead of 85. The remap is applied AFTER the pipeline resize: it is affine and LANCZOS is linear with unit-sum weights so the orders agree in float, but resizing first carries the full range through the filter and only then compresses it.
- Fix applied: `tools/gltf/bake_lost_material_maps.py` (renamed from `bake_lost_basecolor.py`, now covering both slots) and the candidate `exterior_mansion_v6_p25b2.glb`, registered as `?model=p25b2` and **not promoted**. Both controls hold: re-encoding the ORIGINAL base colour AND the ORIGINAL packed metallicRoughness through the same resize and `ktx create` reproduces the bytes v5 ships, sha256 and all. The packing was read out of the shipped file rather than assumed - decoding image 22 gives R=255, **G=roughness (mean 0.5684, matching the source's 0.569)**, B=255. The script asserts both controls and a VERIFY pass, and refuses to write if any drifts.
- **Two instruments were wrong before the numbers meant anything.** Both are worth recording because both produced confident, plausible, false readings.
  1. **The Blender material mask is not the runtime's material footprint.** An unlit-magenta probe of `MAT_Roof_Slate` shows the runtime draws it on 42,762 px where Blender's mask claims 51,352 - **16.9% of the mask is not that material in the runtime at all**, and those pixels sit at mean luma 104.6 against the true roof's 61.0. Every roof figure sampled through the Blender mask alone was contaminated by them, which is why p90/p95/p99 came out *identical to 0.1 luma across three different models*: those pixels could not respond to a change in a material they do not have. On the intersection basis the roof reads 44.0 in Blender, not 50.8.
  2. **WEST and NW were never on their beats.** `WorldCanvas` eases the curve parameter as `curveT(SWING(s))` but reads fov, roll and frameOffset at **raw `s`** (lines 342 vs 345/394). Two consequences: scrolling to a beat's `at` does NOT stand on that beat - WEST needs legProgress **0.408591** and NW **0.532700**, since `SWING(s)=0.2s+0.8*power2.inOut(s)` and GSAP's power2 is CUBIC, not quadratic - and even there the lens is wrong, because at those scrolls `lensAt` returns fov 54.4487/offset 7.5019 for WEST (the beat says 56/8.2) and 52.6757/6.7041 for NW (52/6.4). **A beat's position and its own lens are never on screen together except at s=0 and s=1.** The references were re-rendered at the lens the runtime actually uses.
- Verified, all beats at 1424x900 against Cycles references framed by reproducing this page's own camera maths:
  - **HERO daylight**, on the runtime-verified footprint (Blender mask AND probe, 35,061 px): Blender 44.0; v5 **69.4** (+25.4, MAE 26.7); p25b 64.3 (+20.4, MAE 21.7); p25b2 **61.9** (+18.0, MAE **19.5**, RMS 24.4). p90 83 -> 77 -> **74** against Blender's 68 - the bright end responds once the contaminated pixels are out, which is exactly where a roughness change should act.
  - **WEST daylight** (legProgress 0.4086): roof v5 107.3 -> p25b2 **104.0**. **NW daylight** (legProgress 0.5327): roof v5 97.5 -> p25b2 **96.1**. **DUSK HERO**: roof 100.6 -> **88.9** (-11.7; the rollback grade is more specular, so the remap does more there).
  - **Nothing unrelated moved.** `MAT_Roof` is the control - same slate base colour and the same packed MR image, but its graph has no base-colour multipliers and its roughness Math node is MULTIPLY by 1.0, an identity - and it reads **+0.00 at HERO, WEST, NW and dusk**. Every other material moves by <=0.05 at all four. `MAT_Stone_Trim` appears to move -1.8/-1.5 at WEST/NW; that is instrument #1 again, not a material change, and it is settled structurally: the candidate's JSON differs from v5 in **exactly one material entry**, so the trim material is byte-identical and cannot have changed.
  - Structure: 479 nodes, 381 meshes, 1,785 accessors and the first **9,863,656 bytes** of the buffer byte-identical to v5; two images and two textures appended; `MAT_Roof` still on the originals.
  - Interior route renders and requests `interior_hall.glb` plus the DEFAULT exterior; typecheck 0; 188+48 vitest; lint 0 (pre-existing warnings only - the `ExteriorModel` one is a `useEffect` dep at line 652, untouched); build 0; **0 console errors and 0 warnings** across daylight, dusk and the interior. v5 still hashes `d7a7e945...bc87c3`, p34 and p25b unchanged, `EXTERIOR_MODEL_URL` still v5, locked source blend still mtime 2026-08-27 22:43:35.
- **The residual is now identified, and it is not a material defect.** `mansion_roof` has **936 of its 1,873 faces (50.0%) misoriented** - Blender's own recalculate-outside flips exactly half - plus 356 non-manifold edges. Cycles renders both sides so the source looks correct; three.js honours the exported `doubleSided:false`, which is a FAITHFUL export of `use_backface_culling=True`, and culls them. At HERO that punches an 8,684 px hole in the roof (16.9% of it) through which `MAT_Stone_Wall` is drawn at mean luma 104.6. Ray-casting settles the mechanism: the slate is the FIRST surface along the ray, in front by a median **4.83 m**, with 246 of 250 sampled rays hitting a BACK-facing polygon - so this is not z-fighting and not a missing object. The fix is `Recalculate Outside` on `mansion_roof` in the source; it is geometry authoring, not material authoring, and is out of scope here. Setting `doubleSided:true` would hide it but would contradict the authored flag and paper over the real bug, so it was not done.
- Flagged for review: **yes.** (a) Candidate registered, not promoted. (b) The roof still sits +18.0 above Blender at HERO. Of that, the misoriented-normal hole is the single largest remaining term; what is left after it is the runtime's lack of ray-traced occlusion, which hits every material (`MAT_Stone_Trim`, with no export loss at all, still runs p10 28 -> 54) and is renderer-inherent. (c) The camera finding in instrument #2 is a real inconsistency in `WorldCanvas` - the vantage `curveT`'s own docstring promises is never the one on screen - but the camera is locked, so it is recorded and not touched.

## [2026-09-03 14:35 IST] Phase 2.5B closed: the roof's third defect was the mesh, and the first two measurements were taken through it
- Found: after the base-colour and roughness fixes the roof still read +17.4, and the top decile of it would not move for either fix - identical to 0.1 luma across v5, p25b and p25b2. A lit surface cannot ignore both its albedo and its roughness. It was not ignoring them: **16.9% of the pixels being measured were not that material.** An unlit-magenta probe of MAT_Roof_Slate showed the runtime drawing it over 42,762 px of the 51,352 px Blender assigns it at HERO, and a full 15-material probe identified 96.6% of the 8,684 px difference as MAT_Stone_Wall. The viewer was seeing THROUGH the roof to the wall behind it.
- Why it matters twice over: it is a visible hole in the hero shot, and every roof number in the two entries above was averaged over a region that was 17% cream stone. The corrected figures are in the table below.
- Cause, established rather than assumed. Not z-fighting: ray-casting those pixels puts the slate in front by a **median 4.83 m**, with only 0.4% of samples having a second surface within a millimetre. 246 of 250 rays hit `mansion_roof` on a **BACK-facing** polygon - which for a first intersection from outside a closed surface is impossible unless the winding is wrong. Recalculate-outside flips **936 of its 1873 faces** and the mesh carries 356 non-manifold edges. Cycles shades whichever side a ray lands on, so the Blender reference never showed it. `MAT_Roof_Slate.use_backface_culling` is True, so the exporter wrote `doubleSided: false` **correctly** - three.js was right to cull, and the export was faithful. The defect is in the mesh.
- Fix applied: `normals_make_consistent(inside=False)` on `mansion_roof` alone, in a working copy saved as `mansion_exterior_P25B2-NORMALS.blend`. The locked source is never opened for writing. The script asserts every other mesh's polygon normals are bit-identical afterwards and that vertex positions are untouched - this is an orientation fix, not a remodel.
- **NOT fixed by setting `doubleSided: true`**, which was the tempting one-line answer. That hides the symptom and leaves the tangent basis the normal map is sampled in still mirrored across half the roof. The proof that this matters: correcting the winding is **not a no-op in Blender**. The reference roof moves 50.80 -> 49.21 because tangent space follows winding, and every other material moves by 0.000.
- Delivered as `exterior_mansion_v6_p25b3.glb` (`?model=p25b3`), **not promoted**. Transplanted, not re-exported: the shipped file is Draco-compressed and gltf-transform warns a decode/re-encode round trip is lossy, so a full re-export would perturb all 381 meshes to fix one. `tools/gltf/transplant_mesh.py` writes the corrected primitive uncompressed and drops Draco for that primitive only - **380 of 381 primitives keep their original payload byte for byte**, and the first 10,291,240 bytes of the buffer are identical.
- Verified:
  1. **The hole is closed, measured the same way it was found.** Runtime MAT_Roof_Slate footprint at HERO: **42,762 px -> 51,315 px** against a 51,352 px Blender mask. Missing 16.9% -> **0.3%**, and the 132 px remaining are antialiased silhouette.
  2. **Scene-wide back-face sweep**, 80,100 rays on a 4px grid: back-facing first hits **1,286 -> 697**, and `mansion_roof`'s contribution **590 -> 0**.
  3. **The export path was proven before it was trusted.** A fresh export of the UNFIXED mesh reproduces the shipped one: 6,258 verts and 3,746 tris exactly, and every shipped vertex lies within **0.782 mm** of a control vertex - the Draco quantisation step over a 15.8 m bbox. Bounding boxes agree to 0.000000 m.
  4. **HERO daylight, MAT_Roof_Slate, each candidate against the Blender render of the source it was built from:**

     | | Blender | runtime | delta | MAE | RMS |
     |---|---|---|---|---|---|
     | v5 shipped | 50.8 | 74.5 | +23.7 | 26.1 | 31.0 |
     | p25b +basecolour | 50.8 | 70.2 | +19.4 | 21.9 | 27.7 |
     | p25b2 +roughness | 50.8 | 68.2 | +17.4 | 20.1 | 26.4 |
     | **p25b3 +winding** | **49.2** | **65.7** | **+16.5** | **16.7** | **21.9** |

     Roof MAE **-36%**, RMS **-29%**. Whole-frame MAE 18.40 -> 18.03.
  5. **Nothing unrelated moved.** DUSK HERO across all three fixes: roof **104.1 -> 85.7 (-18.4)** while every other material moves by **<= 0.12**, most by <= 0.04, and 95.3% of changed pixels sit inside the roof mask. NW: every material within **+/-0.63**. WEST: roof 95.9 -> 88.0, MAE 35.7 -> 31.8.
  6. typecheck 0; 266 tests passing (48 public incl. the 29 cameraPath assertions, 188 domain, 30 db); lint 0 (pre-existing warnings only); production build 0 **after clearing `.next`** - a build run against a live dev server fails with `PageNotFoundError` on an opengraph route, which is `.next` contention and not a code fault; 0 console errors and 0 warnings on daylight, dusk and the interior route. The interior route with no query parameter still requests `exterior_mansion_v5.glb`.
  7. v5 still hashes `d7a7e945...bc87c3`; p34, p25b and p25b2 unchanged; `EXTERIOR_MODEL_URL` still resolves v5; locked source blend mtime still 2026-08-27 22:43:35.
- **First-principles reassessment.** Every material's Base Color and Roughness chain was walked and classified against what the exporter can carry. The five stone materials multiply by a Color Attribute, which IS exportable as `COLOR_0` - and all 314 of their primitives carry it, so that path is faithful. `MAT_Stone_Wall` (+43.0), `MAT_Stone_Steps` (+40.0), `MAT_Stone_Paving` (-25.6) and `MAT_Gold` (+54.3) are therefore **correctly exported**, and their residual is the runtime's lighting model: no path-traced GI, and env 0.25 + hemi 0.18 in place of a full HDRI, which redistributes energy from skylit horizontals (paving darker) onto sun-facing verticals (walls brighter). The systemic signature is visible on `MAT_Stone_Trim`, which has no export loss at all and still runs p10 28 -> 54. Touching any of it is forbidden by the mandate and would be chasing a number with the lights.
- Flagged for review: **yes.** Two specific export defects remain, both located and quantified rather than waved off, and both outside this authorisation:
  1. **`MAT_Wood_Dark` base colour** - `MIX` at Fac 0.68 against a constant, unexportable. 5,198 px, 0.41% of frame. Blender's albedo is 3.30x brighter than the shipped texture while the runtime already renders those doors 3.2x brighter than Blender, so the errors oppose and baking it alone takes them 41.0 -> 76.5 against Blender's 12.9. The dominant term is missing contact occlusion on a recessed door.
  2. **`MAT_Stone_Rustic` back-facing faces** - the same defect class as the roof, on the rustic blocks: 90 of 62,516 sampled hits, 0.14%, roughly 1,800 px. `MAT_Gold` and `MAT_Stone_Trim` contribute 4 more samples between them. `hedge_b` accounts for 602 samples but `MAT_Hedge` is `doubleSided: true`, so three.js draws it and it is not a runtime defect.
- Verdict: **PASS for the Phase 2.5B scope.** All three export defects on MAT_Roof_Slate - the two lost base-colour multipliers, the lost roughness remap, and the face winding - are fixed and independently verified. The two residuals above are each below 0.5% of frame, and every other large delta is on a faithfully-exported material.

## [2026-09-03 15:45 IST] Phase 4 / P4A: primary limestone - the wall was a tinted marble scan, and the first two attempts were wrong in instructive ways
- Found (STEP 1 inventory, `docs/PHASE4_MATERIAL_AUDIT.md`): `MAT_Stone_Wall` - 29.7% of the building at HERO - sampled `marble026/color|roughness|normalgl`, not the coursed limestone `make_stone_materials.py` authored for it (on disk, bound to nothing). Effective albedo under the `#E7E3DA` tint was **R/B 2.50** (an orange), roughness mean **0.33 / floor 0.11** (polished), and 69% of tonal energy above 1/64 of the tile - pure micro, no meso. Also found: the runtime multiplies paving/steps/rustic/fountain-trim base colour by **0.32** (daylight) on 106 meshes (`PAVING_TINT`), absent from Blender, so that family was unmeasurable against the reference; and the shipped wall carried `KHR_texture_transform scale 0.3003` (the 3.33 m marble tile), so a Blender tile change is invisible to the runtime unless the transform is rewritten. Owner decisions taken: rebuild the wall on an authored limestone; fold the paving grade into materials (P4B).
- Why a SURFACE and not the coursed tile: each of the 267 ashlar blocks is one physical stone (0.855 x 0.335 m, 0.36 m course pitch) with world UVs. A coursed texture prints sub-courses across single stones and its 405 mm pitch beats against the 360 mm geometry. So `tools/gltf/make_limestone_wall.py` authors bed mottle (0.4-1.2 m), faint bedding (80-110 mm, colour only), sparse pores, roughness 0.55-0.81, no joints - and the MESO layer rides **COLOR_0**: `tools/blender/blocktone_stoneao.py` multiplies each block's StoneAO by a seeded per-object tone (0.90-1.08, darker blocks 1.5% warmer). Zero texture cost, class A export. The shipped ashlar COLOR_0 was 1.0 flat (the AO lives on `mansion_walls`), so the donor loses nothing.
- Three iterations, each measured before the next:
  1. **v1** targeted real limestone albedo (0.54, 0.51, 0.44, R/B 1.23). Under this scene's locked cool environment it rendered **blue-grey in Blender (R/B 0.95)** and **+67 over reference in the runtime**: the marble's heavy warmth had been compensating for the sky, and its low albedo (0.34 luma) for the runtime's direct key. Lesson: author to the RENDERED read under the locked rig, not to a textbook albedo.
  2. **v2** warmed to effective R/B 2.0 and shipped with `uv_scale` written as `KHR_texture_transform`. The runtime showed a dense horizontal stipple across every block that Cycles never showed: the bedding ripple and pore relief in the NORMAL map under a hard grazing key. **v3** moved bedding to colour only, halved pore depth, normal scale 1.2 -> 0.60. Stipple gone.
  3. **v4** albedo x0.93 to land the runtime back at the marble baseline level.
- Diagnosed, not assumed: Blender's wall dropped 71.6 -> 64.2 across the change. Isolation renders show **roughness accounts for -1.7** and the **marble normal map alone for +10.1** (limestone base/rough with the old normal at 1.2 renders 76.4) - a strong high-frequency normal on a grazing-lit face lifts Cycles' mean. So the old Blender reference was flattered by a polished-marble relief the runtime never had.
- Delivered `exterior_mansion_v6_p4a.glb` (`?model=p4a`), **not promoted**, built from p25b3 by `tools/gltf/patch_material_textures.py` (per-slot CONTROL re-encodes the marble sources and reproduces img0/1/2 byte for byte; base ETC1S sRGB, MR ETC1S linear, normal UASTC) and `tools/gltf/transplant_draco_nodes.py` (267 ashlar primitives re-shipped Draco-compressed; every other primitive byte-identical; 380 of 381 primitives keep their payload). Blender side: `mansion_exterior_P4A.blend` via `tools/blender/p4a_wall_material.py` (tint, COLOR_0 multiply, UV projection preserved; other materials asserted bit-identical).
- Verified (HERO daylight, mask basis): wall runtime **114.7 -> 117.3** (+2.6), chroma **R/B 1.55 -> 1.40**, per-block variation present (sd 40.4 -> 42.3); every other material within **+/-0.1** except glass +0.76 (it reflects the wall). 93.1% of changed pixels inside the wall mask. Dusk: wall -1.07 (R/B 1.58 -> 1.47), roof -1.45, rest <= 0.75. WEST: wall +2.0 (R/B 1.43 -> 1.35), glass/gold +2 (reflections), rest <= 0.3. NW: all <= 0.9. Interior untouched. Wall parity vs its own Blender reference is **+53.0 (was +43.0)** - the difference is the normal-map confound above plus the systemic lighting gap, and it is reported rather than chased with the lights.
- Flagged for review: **yes** - (a) the wall is still ~50 luma over Blender at HERO for the same reason trim/steps/gold are: the runtime's key/env split. Out of Phase 4's bounds. (b) `MAT_Stone_Rustic` cannot take per-block tone (96 objects on 2 shared meshes) - P4B handles it in texture. (c) Blender-side references for WEST/NW from the P4A blend are being rendered for P4B.

## [2026-09-03 16:05 IST] Phase 4 / P4B: the stone family, authored once - the runtime paving grade is retired and the family is measured against Blender for the first time
- Found: paving, steps, the 96 rustic blocks and the fountain trim were graded twice - Blender's StoneAO + roughness, then `PAVING_TINT` x0.32 (day) / x0.48 (dusk) on 106 meshes in `ExteriorModel.tsx`. Retired to 1.0 (owner decision, P4 audit §5) and the pass short-circuits, so the runtime runs the GLB's 15 materials instead of 16. Measured ungraded at HERO for the first time: paving **+20.6** over reference, steps **+87.3**, rustic **+54.7**, trim +15.4 (the fountain meshes). Steps and rustic are contact/GI-dominated - Blender puts the steps at 36 under the portico; the runtime has no occlusion there - so albedo cannot close them and this pass does not pretend to.
- Fix applied, textures: `tools/gltf/make_stone_family_p4b.py` reuses `make_stone_materials.build_set` (that module is now guarded by `main()` so it can be imported without rebuilding the shipped v5 sources) and writes `p4b_*` beside them. Trim keeps its colour (it matched Blender to +0.9) and gets a FINISH - roughness 0.38-0.54 against the wall's 0.55-0.81, fine tooling in the micro, a whisper of macro. Paving albedo x0.86, joints darker and dirtier, roughness 0.55-0.76, weathering. Steps x0.82, tread wear up, roughness 0.42-0.60. Rustic x0.82, roughness 0.72-0.92, weathering up. Same tiles as v5, so the shipped `KHR_texture_transform`s stay valid.
- Fix applied, contact: the rustic base had COLOR_0 = 1.0 flat because its 96 blocks share two meshes and a positional bake would be wrong for 95 of them. `tools/blender/rustic_contact_ao.py` writes a block-LOCAL contact term (bed 0.58 rising to 1.0 by 90 mm, top joint 0.90, side joints 0.88; floor 0.40) into StoneAO - correct for every instance because every instance is the same stone the same way up (checked: rotation 0, scale 1, local +Z = world up). The two meshes are re-shipped Draco-compressed by `transplant_draco_nodes.py`, which now dedupes shared meshes and declares only the base's attribute set (the donor carries an unused TEXCOORD_1 the pipeline had pruned).
- Verified, HERO daylight, each runtime against its own Blender reference, both columns ungraded: paving **121.6 -> 113.7** (err +20.6 -> +18.7), rustic **116.0 -> 106.2** (+54.7 -> +51.6), steps **123.6 -> 113.6** (+87.3 -> +81.9), trim 121.7 -> 121.4 (colour untouched by design). 98.6% of changed pixels inside the family masks; every other material moves by **<= 0.01**. Dusk: family +23 to +29 from the grade retirement, everything else 0.00; the dusk paving/wall ratio goes 0.81 -> 1.00. **That is a hierarchy change and it is reported, not hidden**: the old grade held the terrace under the facade at dusk; Blender's own daylight reference puts the terrace ABOVE the wall (1.49), and the material now does neither job by force. WEST through a runtime material-ID mask: wall **-0.00**, roof -0.01, gold -0.00 - the Blender mask at WEST is misregistered (agreement 51-55% for wall/paving, NCC 0.155 at dx -34 in P2.5B) and had shown a spurious wall +10.5 in the lower rows where its footprint overlaps the brightened terrace. NW: cypress/hedge/roof/gold 0.00 (pose proven), family +3.8 to +5.8; the glass/wall readings there carry the same mask limitation. typecheck 0.
- Flagged for review: **yes** - (a) the terrace no longer sits under the facade at dusk (1.00 vs 0.81 graded); if that art direction is wanted back it belongs in the material as a deliberate value, not in a multiply the reference cannot see. (b) `exterior_mansion_v6_p4b.glb` is 15.9 MB because superseded images stay in the buffer (15 orphans across p4a/p4b); an orphan-prune step is written for the final candidate. (c) The unlit probe classifier fails for green/cyan hues under ACES (cypress/hedge/glass read 0 px at WEST); it is reliable for the stone/roof/gold set it was built for and needs a hue-angle classifier before it is used for foliage.

## [2026-09-03 16:20 IST] Phase 4 / P4C: roof slate and the dark-oak door - one lever landed, one did not, and both are measured
- Found: `MAT_Wood_Dark` shipped a small-plank parquet scan (planks ~12 cm at the door's 1 m/UV) with a flat 0.33 roughness and a flat normal, sampled through an object-space mapping and a legacy MIX the exporter could not see (P2.5B). `MAT_Roof_Slate` still carried a MapRange and two legacy MixRGB nodes in its Blender graph even after the P2.5B bake, so Blender and glTF read different chains.
- Fix applied, wood: `tools/gltf/make_p4c_roof_wood.py` authors a dark stained oak - continuous vertical grain (the door's panels are geometry, `door_relief`), linear albedo ~(0.078, 0.052, 0.034), roughness 0.36-0.56 following the grain, a subtle open-grain normal at 0.60 - sampled through UV in both renderers (`tools/blender/p4c_roof_wood_material.py`). Blender's authored mix was 0.072 luma, the shipped raw texture 0.022; this is 0.055. The runtime door reads **41.1 -> 54.5** at HERO daylight and 124.5 -> 135.8 at dusk (the dusk value is the runtime's own EMISSIVE grade on the door). That brightening is the material being a real dark wood instead of a near-black; parity vs Blender widens from +28.7 to +45.4 because Blender's door sits in contact shadow the runtime does not have (P2.5B), and it is reported, not hidden.
- Fix applied, roof: re-derived from the same sources and maths as the P2.5B bake with AO weight 0.72, a per-slate tone/roughness offset keyed off the 16-bit height plateaus (7,535 slates), a 3% graphite shift; roughness now ships ABSOLUTE with roughnessFactor 1.0 and the Blender graph reads the same two maps - no MapRange or MixRGB left. Mean linear luma 0.944x the P2.5B bake.
- **Measured honestly: the roof readability lever did not land.** Band-pass local contrast inside the roof mask - HERO (sigma 1.5..8 px): runtime 6.45 -> 6.53, Blender 7.59 -> 7.56. WEST at 6 m (sigma 3..16 px): runtime 15.56 -> 15.64, Blender 9.89 -> 9.98. At the hero distance a slate is ~3 px and the 1024/ETC1S/mip chain averages the courses regardless of source contrast; at 6 m the contrast is already there. The change is kept for the chain cleanup and the per-slate variation, at a cost of -0.9 luma, and the finding is that texture-level slate readability at HERO is resolution-limited, not authoring-limited.
- Verified: HERO p4b -> p4c changes 1.23% of the frame, 87.2% inside the roof+wood masks; every other material within +/-0.02. Dusk: roof -0.79, wood +11.27, all else <= 0.06. WEST: roof -0.58, all else <= 0.17 (0.84% of frame changed). Wood controls re-encode `wood_dark/*` and reproduce img9/10/11 byte for byte.
- Flagged for review: **yes** - (a) the door is brighter than v5 by design; if the owner prefers the darker read, the albedo mean is one constant in the generator. (b) The candidate is 17.5 MB: 20 superseded images are still in the buffer across p4a/b/c; the orphan-prune step is next.

## [2026-09-03 16:40 IST] Phase 4 / P4D: gold and water restraint - factors only
- Found: `MAT_Gold` multiplied gilt_aged/roughness (mean 0.28) by 0.55 - an effective ~0.15, a mirror - and was the brightest surface in the daylight frame (138.7 against Blender's 83.5, +55). `MAT_Water` roughness 0.02 read as a mirror plate.
- Fix applied: roughnessFactor 0.55 -> 0.90 and an aged-gilt baseColorFactor (0.85, 0.80, 0.70) on the gold; water roughness 0.02 -> 0.07 with ior 1.333 / transmission untouched. Mirrored in Blender by `tools/blender/p4d_accents_material.py` through a modern Mix (MULTIPLY, Fac 1) and the existing Math scalar - both exporter-visible - so the reference and the runtime carry the same numbers. Glass deliberately left alone (audit class D). The GLB buffer is byte-identical to p4c; only two material records differ.
- Verified: HERO daylight gold **138.7 -> 132.0** (err +55.7 -> +50.8), water 16.4 -> 16.4 (the daylight fountain is transmission-dominated at this angle), every other material within +/-0.18 (the -0.18 on MAT_Roof is the finials' reflection). Dusk: gold 198.0 -> 188.7, water -0.24, all else <= 0.15. 0.57% of the frame changed at HERO.
- Flagged for review: the gold is still +50 over Blender in daylight because most of its runtime brightness is the key's specular on a metal, which no roughness in a believable brass range removes; the remaining lever is the key/env split, which is out of Phase 4's bounds.

## [2026-09-03 17:00 IST] Phase 4 / P4E: the assembled surface system, pruned, regression-tested - PASS with stated residuals
- Fix applied: `tools/gltf/prune_orphan_images.py` drops the 20 images (6.31 MB) the four milestone patches left in the buffer, rebuilds the BIN without holes and re-indexes, verifying material->image bindings and mesh attribute sets unchanged before writing. `exterior_mansion_v6_p4e.glb` = p4d's materials and geometry at **11.04 MB** (v5 9.72; KTX2 payload 8.63 -> 9.60 MB, the wall's and door's UASTC normals being the cost). Registered as `?model=p4e`, **not promoted**.
- Verified: p4d -> p4e runtime capture mean abs diff 0.049/255 (the animation floor). Fresh production build on a clean `.next` + fresh browser context: `?model=p4e` **0 console errors / 0 warnings**, `/hall` **0 / 0** with `interior_hall.glb` unchanged and the default route still requesting v5. typecheck 5/5; 266 tests; lint 0 errors (pre-existing warnings only); build 2/2. v5 `d7a7e945...bc87c3`, p31-p34, p25b-p25b3, interior and the locked blend all unchanged. Runtime materials 16 -> 15 (paving clones gone), GLB materials 15, draw calls unchanged. Frame rate NOT MEASURABLE here (headless rAF 1.3/s) and not reported.
- Full before/after, per-beat and per-grade tables, the export transformations and the residuals are in `docs/PHASE4_REPORT.md`. Verdict there: **PASS** for the Phase 4 scope - export-safe (every replaced slot control-verified byte for byte), regression-tested (unrelated materials <= 0.2 at every step), visually coherent - with seven stated residuals, the largest of which (wall/steps/rustic/gold parity) are the runtime's key/env split and are locked variables.
- Flagged for review: promotion is the owner's call; `EXTERIOR_MODEL_URL` still resolves v5.
